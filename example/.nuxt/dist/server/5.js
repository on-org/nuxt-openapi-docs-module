exports.ids = [5];
exports.modules = {

/***/ 102:
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _example_node_modules_vue_style_loader_index_js_ref_3_oneOf_1_0_example_node_modules_css_loader_dist_cjs_js_ref_3_oneOf_1_1_example_node_modules_vue_loader_lib_loaders_stylePostLoader_js_example_node_modules_postcss_loader_dist_cjs_js_ref_3_oneOf_1_2_example_node_modules_nuxt_components_dist_loader_js_ref_0_0_example_node_modules_vue_loader_lib_index_js_vue_loader_options_CodeView_vue_vue_type_style_index_0_id_8d539996_prod_lang_css___WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(65);
/* harmony import */ var _example_node_modules_vue_style_loader_index_js_ref_3_oneOf_1_0_example_node_modules_css_loader_dist_cjs_js_ref_3_oneOf_1_1_example_node_modules_vue_loader_lib_loaders_stylePostLoader_js_example_node_modules_postcss_loader_dist_cjs_js_ref_3_oneOf_1_2_example_node_modules_nuxt_components_dist_loader_js_ref_0_0_example_node_modules_vue_loader_lib_index_js_vue_loader_options_CodeView_vue_vue_type_style_index_0_id_8d539996_prod_lang_css___WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_example_node_modules_vue_style_loader_index_js_ref_3_oneOf_1_0_example_node_modules_css_loader_dist_cjs_js_ref_3_oneOf_1_1_example_node_modules_vue_loader_lib_loaders_stylePostLoader_js_example_node_modules_postcss_loader_dist_cjs_js_ref_3_oneOf_1_2_example_node_modules_nuxt_components_dist_loader_js_ref_0_0_example_node_modules_vue_loader_lib_index_js_vue_loader_options_CodeView_vue_vue_type_style_index_0_id_8d539996_prod_lang_css___WEBPACK_IMPORTED_MODULE_0__);
/* harmony reexport (unknown) */ for(var __WEBPACK_IMPORT_KEY__ in _example_node_modules_vue_style_loader_index_js_ref_3_oneOf_1_0_example_node_modules_css_loader_dist_cjs_js_ref_3_oneOf_1_1_example_node_modules_vue_loader_lib_loaders_stylePostLoader_js_example_node_modules_postcss_loader_dist_cjs_js_ref_3_oneOf_1_2_example_node_modules_nuxt_components_dist_loader_js_ref_0_0_example_node_modules_vue_loader_lib_index_js_vue_loader_options_CodeView_vue_vue_type_style_index_0_id_8d539996_prod_lang_css___WEBPACK_IMPORTED_MODULE_0__) if(["default"].indexOf(__WEBPACK_IMPORT_KEY__) < 0) (function(key) { __webpack_require__.d(__webpack_exports__, key, function() { return _example_node_modules_vue_style_loader_index_js_ref_3_oneOf_1_0_example_node_modules_css_loader_dist_cjs_js_ref_3_oneOf_1_1_example_node_modules_vue_loader_lib_loaders_stylePostLoader_js_example_node_modules_postcss_loader_dist_cjs_js_ref_3_oneOf_1_2_example_node_modules_nuxt_components_dist_loader_js_ref_0_0_example_node_modules_vue_loader_lib_index_js_vue_loader_options_CodeView_vue_vue_type_style_index_0_id_8d539996_prod_lang_css___WEBPACK_IMPORTED_MODULE_0__[key]; }) }(__WEBPACK_IMPORT_KEY__));


/***/ }),

/***/ 103:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(6);
var ___CSS_LOADER_EXPORT___ = ___CSS_LOADER_API_IMPORT___(function(i){return i[1]});
// Module
___CSS_LOADER_EXPORT___.push([module.i, "pre code.hljs{display:block;overflow-x:auto;padding:1em}code.hljs{padding:3px 5px}/*!\n  Theme: Tokyo-night-Dark\n  origin: https://github.com/enkia/tokyo-night-vscode-theme\n  Description: Original highlight.js style\n  Author: (c) Henri Vandersleyen <hvandersleyen@gmail.com>\n  License: see project LICENSE\n  Touched: 2022\n*/.hljs-comment,.hljs-meta{color:#565f89}.hljs-deletion,.hljs-doctag,.hljs-regexp,.hljs-selector-attr,.hljs-selector-class,.hljs-selector-id,.hljs-selector-pseudo,.hljs-tag,.hljs-template-tag,.hljs-variable.language_{color:#f7768e}.hljs-link,.hljs-literal,.hljs-number,.hljs-params,.hljs-template-variable,.hljs-type,.hljs-variable{color:#ff9e64}.hljs-attribute,.hljs-built_in{color:#e0af68}.hljs-keyword,.hljs-property,.hljs-subst,.hljs-title,.hljs-title.class_,.hljs-title.class_.inherited__,.hljs-title.function_{color:#7dcfff}.hljs-selector-tag{color:#73daca}.hljs-addition,.hljs-bullet,.hljs-quote,.hljs-string,.hljs-symbol{color:#9ece6a}.hljs-code,.hljs-formula,.hljs-section{color:#7aa2f7}.hljs-attr,.hljs-char.escape_,.hljs-keyword,.hljs-name,.hljs-operator{color:#bb9af7}.hljs-punctuation{color:#c0caf5}.hljs{background:#1a1b26;color:#9aa5ce}.hljs-emphasis{font-style:italic}.hljs-strong{font-weight:700}:not(pre)>code[class*=language-],pre[class*=language-]{background:#001529;color:#d3d3d3}.toolbar-btn{background-color:#00a2fb;border:none;color:#fff;cursor:pointer;font-size:12px;margin:0 2px;min-width:50px;padding:4px;right:10px;top:10px;z-index:99}", ""]);
// Exports
___CSS_LOADER_EXPORT___.locals = {};
module.exports = ___CSS_LOADER_EXPORT___;


/***/ }),

/***/ 104:
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _example_node_modules_vue_style_loader_index_js_ref_3_oneOf_1_0_example_node_modules_css_loader_dist_cjs_js_ref_3_oneOf_1_1_example_node_modules_vue_loader_lib_loaders_stylePostLoader_js_example_node_modules_postcss_loader_dist_cjs_js_ref_3_oneOf_1_2_example_node_modules_nuxt_components_dist_loader_js_ref_0_0_example_node_modules_vue_loader_lib_index_js_vue_loader_options_CustomDropdownWithSubMenu_vue_vue_type_style_index_0_id_2fc26aab_prod_scoped_true_lang_css___WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(66);
/* harmony import */ var _example_node_modules_vue_style_loader_index_js_ref_3_oneOf_1_0_example_node_modules_css_loader_dist_cjs_js_ref_3_oneOf_1_1_example_node_modules_vue_loader_lib_loaders_stylePostLoader_js_example_node_modules_postcss_loader_dist_cjs_js_ref_3_oneOf_1_2_example_node_modules_nuxt_components_dist_loader_js_ref_0_0_example_node_modules_vue_loader_lib_index_js_vue_loader_options_CustomDropdownWithSubMenu_vue_vue_type_style_index_0_id_2fc26aab_prod_scoped_true_lang_css___WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_example_node_modules_vue_style_loader_index_js_ref_3_oneOf_1_0_example_node_modules_css_loader_dist_cjs_js_ref_3_oneOf_1_1_example_node_modules_vue_loader_lib_loaders_stylePostLoader_js_example_node_modules_postcss_loader_dist_cjs_js_ref_3_oneOf_1_2_example_node_modules_nuxt_components_dist_loader_js_ref_0_0_example_node_modules_vue_loader_lib_index_js_vue_loader_options_CustomDropdownWithSubMenu_vue_vue_type_style_index_0_id_2fc26aab_prod_scoped_true_lang_css___WEBPACK_IMPORTED_MODULE_0__);
/* harmony reexport (unknown) */ for(var __WEBPACK_IMPORT_KEY__ in _example_node_modules_vue_style_loader_index_js_ref_3_oneOf_1_0_example_node_modules_css_loader_dist_cjs_js_ref_3_oneOf_1_1_example_node_modules_vue_loader_lib_loaders_stylePostLoader_js_example_node_modules_postcss_loader_dist_cjs_js_ref_3_oneOf_1_2_example_node_modules_nuxt_components_dist_loader_js_ref_0_0_example_node_modules_vue_loader_lib_index_js_vue_loader_options_CustomDropdownWithSubMenu_vue_vue_type_style_index_0_id_2fc26aab_prod_scoped_true_lang_css___WEBPACK_IMPORTED_MODULE_0__) if(["default"].indexOf(__WEBPACK_IMPORT_KEY__) < 0) (function(key) { __webpack_require__.d(__webpack_exports__, key, function() { return _example_node_modules_vue_style_loader_index_js_ref_3_oneOf_1_0_example_node_modules_css_loader_dist_cjs_js_ref_3_oneOf_1_1_example_node_modules_vue_loader_lib_loaders_stylePostLoader_js_example_node_modules_postcss_loader_dist_cjs_js_ref_3_oneOf_1_2_example_node_modules_nuxt_components_dist_loader_js_ref_0_0_example_node_modules_vue_loader_lib_index_js_vue_loader_options_CustomDropdownWithSubMenu_vue_vue_type_style_index_0_id_2fc26aab_prod_scoped_true_lang_css___WEBPACK_IMPORTED_MODULE_0__[key]; }) }(__WEBPACK_IMPORT_KEY__));


/***/ }),

/***/ 105:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(6);
var ___CSS_LOADER_EXPORT___ = ___CSS_LOADER_API_IMPORT___(function(i){return i[1]});
// Module
___CSS_LOADER_EXPORT___.push([module.i, "button[data-v-2fc26aab]:focus-visible{outline:2px solid rgba(156,163,175,var(--tw-text-opacity));outline-offset:2px}ul[role=menu][data-v-2fc26aab]{z-index:20}.dropdown-enter-active[data-v-2fc26aab],.dropdown-leave-active[data-v-2fc26aab]{transition:opacity .2s ease,transform .2s ease}.dropdown-enter[data-v-2fc26aab],.dropdown-enter-active[data-v-2fc26aab],.dropdown-leave-active[data-v-2fc26aab],.dropdown-leave-to[data-v-2fc26aab]{opacity:0;transform:translateY(-.5rem) scale(.98)}.dropdown-list[data-v-2fc26aab]{max-height:300px;overflow-x:auto}.sub-menu[data-v-2fc26aab]{margin:0}.menu-item[data-v-2fc26aab]{border:1px solid #eaeaea;cursor:pointer}.sub-menu-item[data-v-2fc26aab]{background-color:#f6f6f6;cursor:pointer}.sub-menu-item[data-v-2fc26aab]:hover{background-color:#e8e8e8}@media (max-width:639px){.dropdown-enter-active[data-v-2fc26aab],.dropdown-leave-active[data-v-2fc26aab]{transition:opacity .2s ease-out,transform .2s ease-out}.dropdown-enter[data-v-2fc26aab],.dropdown-leave-to[data-v-2fc26aab]{opacity:0;transform:translateY(-20px)}.dropdown-leave-active[data-v-2fc26aab]{position:absolute;width:100%}.dropdown-list[data-v-2fc26aab]{background-color:#fff;bottom:0;left:0;position:absolute;right:0;top:0;z-index:-1}}", ""]);
// Exports
___CSS_LOADER_EXPORT___.locals = {};
module.exports = ___CSS_LOADER_EXPORT___;


/***/ }),

/***/ 106:
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _example_node_modules_vue_style_loader_index_js_ref_3_oneOf_1_0_example_node_modules_css_loader_dist_cjs_js_ref_3_oneOf_1_1_example_node_modules_vue_loader_lib_loaders_stylePostLoader_js_example_node_modules_postcss_loader_dist_cjs_js_ref_3_oneOf_1_2_example_node_modules_nuxt_components_dist_loader_js_ref_0_0_example_node_modules_vue_loader_lib_index_js_vue_loader_options_CodeSimples_vue_vue_type_style_index_0_id_f8cd87ea_prod_lang_css___WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(67);
/* harmony import */ var _example_node_modules_vue_style_loader_index_js_ref_3_oneOf_1_0_example_node_modules_css_loader_dist_cjs_js_ref_3_oneOf_1_1_example_node_modules_vue_loader_lib_loaders_stylePostLoader_js_example_node_modules_postcss_loader_dist_cjs_js_ref_3_oneOf_1_2_example_node_modules_nuxt_components_dist_loader_js_ref_0_0_example_node_modules_vue_loader_lib_index_js_vue_loader_options_CodeSimples_vue_vue_type_style_index_0_id_f8cd87ea_prod_lang_css___WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_example_node_modules_vue_style_loader_index_js_ref_3_oneOf_1_0_example_node_modules_css_loader_dist_cjs_js_ref_3_oneOf_1_1_example_node_modules_vue_loader_lib_loaders_stylePostLoader_js_example_node_modules_postcss_loader_dist_cjs_js_ref_3_oneOf_1_2_example_node_modules_nuxt_components_dist_loader_js_ref_0_0_example_node_modules_vue_loader_lib_index_js_vue_loader_options_CodeSimples_vue_vue_type_style_index_0_id_f8cd87ea_prod_lang_css___WEBPACK_IMPORTED_MODULE_0__);
/* harmony reexport (unknown) */ for(var __WEBPACK_IMPORT_KEY__ in _example_node_modules_vue_style_loader_index_js_ref_3_oneOf_1_0_example_node_modules_css_loader_dist_cjs_js_ref_3_oneOf_1_1_example_node_modules_vue_loader_lib_loaders_stylePostLoader_js_example_node_modules_postcss_loader_dist_cjs_js_ref_3_oneOf_1_2_example_node_modules_nuxt_components_dist_loader_js_ref_0_0_example_node_modules_vue_loader_lib_index_js_vue_loader_options_CodeSimples_vue_vue_type_style_index_0_id_f8cd87ea_prod_lang_css___WEBPACK_IMPORTED_MODULE_0__) if(["default"].indexOf(__WEBPACK_IMPORT_KEY__) < 0) (function(key) { __webpack_require__.d(__webpack_exports__, key, function() { return _example_node_modules_vue_style_loader_index_js_ref_3_oneOf_1_0_example_node_modules_css_loader_dist_cjs_js_ref_3_oneOf_1_1_example_node_modules_vue_loader_lib_loaders_stylePostLoader_js_example_node_modules_postcss_loader_dist_cjs_js_ref_3_oneOf_1_2_example_node_modules_nuxt_components_dist_loader_js_ref_0_0_example_node_modules_vue_loader_lib_index_js_vue_loader_options_CodeSimples_vue_vue_type_style_index_0_id_f8cd87ea_prod_lang_css___WEBPACK_IMPORTED_MODULE_0__[key]; }) }(__WEBPACK_IMPORT_KEY__));


/***/ }),

/***/ 107:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(6);
var ___CSS_LOADER_EXPORT___ = ___CSS_LOADER_API_IMPORT___(function(i){return i[1]});
// Module
___CSS_LOADER_EXPORT___.push([module.i, "pre{background-color:#f7f7f7;border-radius:5px;overflow-x:auto;padding:10px}code,pre{font-family:monospace}.keyword{color:#0077c2;font-weight:700}.operator{color:#a90d91}.string{color:#2a9d8f}.punctuation{color:#a90d91}", ""]);
// Exports
___CSS_LOADER_EXPORT___.locals = {};
module.exports = ___CSS_LOADER_EXPORT___;


/***/ }),

/***/ 111:
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
// ESM COMPAT FLAG
__webpack_require__.r(__webpack_exports__);

// CONCATENATED MODULE: ./node_modules/babel-loader/lib??ref--2-0!./node_modules/vue-loader/lib/loaders/templateLoader.js??ref--6!./node_modules/@nuxt/components/dist/loader.js??ref--0-0!./node_modules/vue-loader/lib??vue-loader-options!../components/lib/CodeSimples.vue?vue&type=template&id=f8cd87ea&
var render = function render() {
  var _vm = this,
    _c = _vm._self._c;
  return _c('div', {
    staticClass: "col regular-font request-panel code-simple"
  }, [_c('CustomDropdownWithSubMenu', {
    attrs: {
      "items": _vm.configs
    },
    on: {
      "select": _vm.onLangClick
    }
  }), _vm._ssrNode(" "), _c('CodeView', {
    attrs: {
      "lang": _vm.snippet,
      "code": _vm.code
    }
  })], 2);
};
var staticRenderFns = [];

// CONCATENATED MODULE: ../components/lib/CodeSimples.vue?vue&type=template&id=f8cd87ea&

// CONCATENATED MODULE: ./node_modules/babel-loader/lib??ref--2-0!./node_modules/vue-loader/lib/loaders/templateLoader.js??ref--6!./node_modules/@nuxt/components/dist/loader.js??ref--0-0!./node_modules/vue-loader/lib??vue-loader-options!../components/lib/CodeView.vue?vue&type=template&id=8d539996&
var CodeViewvue_type_template_id_8d539996_render = function render() {
  var _vm = this,
    _c = _vm._self._c;
  return _c('div', {
    staticClass: "code-panel"
  }, [_vm._ssrNode("<div class=\"code-panel-body relative\"><button class=\"toolbar-btn absolute top-2 right-2\">Copy</button> <pre" + _vm._ssrClass("language line-numbers", `language-${_vm.lang}`) + "><code class=\"language\">" + _vm._s(_vm.html) + "</code></pre></div>")]);
};
var CodeViewvue_type_template_id_8d539996_staticRenderFns = [];

// CONCATENATED MODULE: ../components/lib/CodeView.vue?vue&type=template&id=8d539996&

// EXTERNAL MODULE: external "highlight.js/lib/core"
var core_ = __webpack_require__(20);
var core_default = /*#__PURE__*/__webpack_require__.n(core_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/javascript"
var javascript_ = __webpack_require__(21);
var javascript_default = /*#__PURE__*/__webpack_require__.n(javascript_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/java"
var java_ = __webpack_require__(22);
var java_default = /*#__PURE__*/__webpack_require__.n(java_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/clojure"
var clojure_ = __webpack_require__(23);
var clojure_default = /*#__PURE__*/__webpack_require__.n(clojure_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/csharp"
var csharp_ = __webpack_require__(24);
var csharp_default = /*#__PURE__*/__webpack_require__.n(csharp_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/http"
var http_ = __webpack_require__(25);
var http_default = /*#__PURE__*/__webpack_require__.n(http_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/kotlin"
var kotlin_ = __webpack_require__(26);
var kotlin_default = /*#__PURE__*/__webpack_require__.n(kotlin_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/powershell"
var powershell_ = __webpack_require__(27);
var powershell_default = /*#__PURE__*/__webpack_require__.n(powershell_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/r"
var r_ = __webpack_require__(28);
var r_default = /*#__PURE__*/__webpack_require__.n(r_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/ruby"
var ruby_ = __webpack_require__(29);
var ruby_default = /*#__PURE__*/__webpack_require__.n(ruby_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/rust"
var rust_ = __webpack_require__(30);
var rust_default = /*#__PURE__*/__webpack_require__.n(rust_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/swift"
var swift_ = __webpack_require__(31);
var swift_default = /*#__PURE__*/__webpack_require__.n(swift_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/bash"
var bash_ = __webpack_require__(32);
var bash_default = /*#__PURE__*/__webpack_require__.n(bash_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/ocaml"
var ocaml_ = __webpack_require__(33);
var ocaml_default = /*#__PURE__*/__webpack_require__.n(ocaml_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/python"
var python_ = __webpack_require__(34);
var python_default = /*#__PURE__*/__webpack_require__.n(python_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/json"
var json_ = __webpack_require__(35);
var json_default = /*#__PURE__*/__webpack_require__.n(json_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/go"
var go_ = __webpack_require__(36);
var go_default = /*#__PURE__*/__webpack_require__.n(go_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/php"
var php_ = __webpack_require__(37);
var php_default = /*#__PURE__*/__webpack_require__.n(php_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/c"
var c_ = __webpack_require__(38);
var c_default = /*#__PURE__*/__webpack_require__.n(c_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/brainfuck"
var brainfuck_ = __webpack_require__(39);
var brainfuck_default = /*#__PURE__*/__webpack_require__.n(brainfuck_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/lua"
var lua_ = __webpack_require__(40);
var lua_default = /*#__PURE__*/__webpack_require__.n(lua_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/perl"
var perl_ = __webpack_require__(41);
var perl_default = /*#__PURE__*/__webpack_require__.n(perl_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/cpp"
var cpp_ = __webpack_require__(42);
var cpp_default = /*#__PURE__*/__webpack_require__.n(cpp_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/scala"
var scala_ = __webpack_require__(43);
var scala_default = /*#__PURE__*/__webpack_require__.n(scala_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/dart"
var dart_ = __webpack_require__(44);
var dart_default = /*#__PURE__*/__webpack_require__.n(dart_);

// EXTERNAL MODULE: external "highlight.js/lib/languages/objectivec"
var objectivec_ = __webpack_require__(45);
var objectivec_default = /*#__PURE__*/__webpack_require__.n(objectivec_);

// EXTERNAL MODULE: ../components/helpers.js
var helpers = __webpack_require__(46);

// CONCATENATED MODULE: ./node_modules/babel-loader/lib??ref--2-0!./node_modules/@nuxt/components/dist/loader.js??ref--0-0!./node_modules/vue-loader/lib??vue-loader-options!../components/lib/CodeView.vue?vue&type=script&lang=js&
/** Mode */
 /** Languages */

























core_default.a.registerLanguage('javascript', javascript_default.a);
core_default.a.registerLanguage('java', java_default.a);
core_default.a.registerLanguage('clojure', clojure_default.a);
core_default.a.registerLanguage('csharp', csharp_default.a);
core_default.a.registerLanguage('http', http_default.a);
core_default.a.registerLanguage('kotlin', kotlin_default.a);
core_default.a.registerLanguage('powershell', powershell_default.a);
core_default.a.registerLanguage('r', r_default.a);
core_default.a.registerLanguage('ruby', ruby_default.a);
core_default.a.registerLanguage('rust', rust_default.a);
core_default.a.registerLanguage('swift', swift_default.a);
core_default.a.registerLanguage('bash', bash_default.a);
core_default.a.registerLanguage('ocaml', ocaml_default.a);
core_default.a.registerLanguage('python', python_default.a);
core_default.a.registerLanguage('json', json_default.a);
core_default.a.registerLanguage('go', go_default.a);
core_default.a.registerLanguage('php', php_default.a);
core_default.a.registerLanguage('c', c_default.a);
core_default.a.registerLanguage('brainfuck', brainfuck_default.a);
core_default.a.registerLanguage('lua', lua_default.a);
core_default.a.registerLanguage('perl', perl_default.a);
core_default.a.registerLanguage('cpp', cpp_default.a);
core_default.a.registerLanguage('scala', scala_default.a);
core_default.a.registerLanguage('dart', dart_default.a);
core_default.a.registerLanguage('objectivec', objectivec_default.a);

/* harmony default export */ var CodeViewvue_type_script_lang_js_ = ({
  name: 'code-view',
  props: {
    code: {
      type: String,
      required: true
    },
    lang: {
      type: String,
      required: false,
      default: ''
    }
  },
  data() {
    return {
      html: 'javascript'
    };
  },
  watch: {
    code() {
      this.toHtml();
    },
    lang() {
      this.toHtml();
    }
  },
  methods: {
    copyToClipboard(e) {
      Object(helpers["a" /* copyToClipboard */])(this.code, e);
    },
    toHtml() {
      const language = this.lang || 'javascript';
      const grammar = core_default.a.getLanguage(language);
      if (!grammar) {
        this.html = this.code;
      }
      this.html = core_default.a.highlight(this.code, {
        language,
        ignoreIllegals: true
      }).value;
    }
  },
  mounted() {
    this.toHtml();
  }
});
// CONCATENATED MODULE: ../components/lib/CodeView.vue?vue&type=script&lang=js&
 /* harmony default export */ var lib_CodeViewvue_type_script_lang_js_ = (CodeViewvue_type_script_lang_js_); 
// EXTERNAL MODULE: ./node_modules/vue-loader/lib/runtime/componentNormalizer.js
var componentNormalizer = __webpack_require__(2);

// CONCATENATED MODULE: ../components/lib/CodeView.vue



function injectStyles (context) {
  
  var style0 = __webpack_require__(102)
if (style0.__inject__) style0.__inject__(context)

}

/* normalize component */

var component = Object(componentNormalizer["a" /* default */])(
  lib_CodeViewvue_type_script_lang_js_,
  CodeViewvue_type_template_id_8d539996_render,
  CodeViewvue_type_template_id_8d539996_staticRenderFns,
  false,
  injectStyles,
  null,
  "7d888495"
  
)

/* harmony default export */ var CodeView = (component.exports);
// CONCATENATED MODULE: ./node_modules/babel-loader/lib??ref--2-0!./node_modules/vue-loader/lib/loaders/templateLoader.js??ref--6!./node_modules/@nuxt/components/dist/loader.js??ref--0-0!./node_modules/vue-loader/lib??vue-loader-options!../components/lib/CustomDropdownWithSubMenu.vue?vue&type=template&id=2fc26aab&scoped=true&
var CustomDropdownWithSubMenuvue_type_template_id_2fc26aab_scoped_true_render = function render() {
  var _vm = this,
    _c = _vm._self._c;
  return _c('div', {
    staticClass: "relative"
  }, [_vm._ssrNode("<button type=\"button\" aria-haspopup=\"true\"" + _vm._ssrAttr("aria-expanded", _vm.isOpen) + " class=\"inline-flex justify-center items-center w-full rounded-md border border-gray-300 shadow-sm px-4 py-2 bg-white text-sm font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 dark:bg-black dark:text-gray-300/75\" data-v-2fc26aab><b data-v-2fc26aab>" + _vm._ssrEscape(_vm._s(_vm.selectedText ? _vm.selectedText : 'Select Library')) + "</b> <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 0 20 20\" fill=\"currentColor\" aria-hidden=\"true\"" + _vm._ssrClass("-mr-1 ml-2 h-5 w-5", _vm.isOpen ? '-rotate-180' : 'rotate-0') + " data-v-2fc26aab><path fill-rule=\"evenodd\" d=\"M5.293 7.707a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 011.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z\" clip-rule=\"evenodd\" data-v-2fc26aab></path></svg></button> "), _c('transition', {
    attrs: {
      "name": "dropdown"
    }
  }, [_vm.isOpen ? _c('ul', {
    staticClass: "dropdown-list origin-top-right absolute left-0 mt-2 ml-0 w-56 rounded-md shadow-lg bg-white dark:bg-black dark:text-gray-300/75 ring-1 ring-black ring-opacity-5 focus:outline-none",
    attrs: {
      "role": "menu",
      "aria-orientation": "vertical",
      "aria-labelledby": "options-menu"
    }
  }, _vm._l(_vm.items, function (item, index) {
    return _c('li', {
      key: index,
      staticClass: "border block px-4 py-2 m-0 text-sm text-gray-700 hover:bg-white hover:text-gray-800 dark:bg-black dark:hover:bg-gray-800 dark:text-gray-300/75",
      attrs: {
        "role": "menuitem"
      },
      on: {
        "click": function ($event) {
          return _vm.handleItemClick(item, index);
        }
      }
    }, [item.libraries ? [_c('div', {
      staticClass: "flex justify-between cursor-pointer"
    }, [_c('span', {
      staticClass: "flex flex-grow"
    }, [_vm._v(_vm._s(item.snippet))]), _vm._v(" "), _c('span', {
      staticClass: "flex flex-grow",
      staticStyle: {
        "justify-content": "end"
      }
    }, [_c('svg', {
      staticClass: "h-4 w-4 ml-2 self-center",
      staticStyle: {
        "display": "initial"
      },
      attrs: {
        "viewBox": "0 0 20 20",
        "fill": "currentColor"
      }
    }, [_c('path', {
      attrs: {
        "fill-rule": "evenodd",
        "d": "M5.293 7.293a1 1 0 011.414 0L10 10.586l2.293-2.293a1 1 0 011.414 1.414l-3 3a1 1 0 01-1.414 0l-3-3a1 1 0 010-1.414z",
        "clip-rule": "evenodd"
      }
    })])])]), _vm._v(" "), _vm.selectedSnippet === index ? _c('ul', {
      staticClass: "sub-menu"
    }, _vm._l(item.libraries, function (library, libraryKey) {
      return _c('li', {
        key: libraryKey,
        staticClass: "bg-gray-100 hover:bg-gray-200 dark:bg-black dark:hover:bg-gray-800 dark:text-gray-300/75 block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 hover:text-gray-900",
        on: {
          "click": function ($event) {
            return _vm.handleItemClick(item, index, libraryKey);
          }
        }
      }, [_vm._v("\n              " + _vm._s(library) + "\n            ")]);
    }), 0) : _vm._e()] : [_vm._v("\n          " + _vm._s(item.snippet) + "\n        ")]], 2);
  }), 0) : _vm._e()])], 2);
};
var CustomDropdownWithSubMenuvue_type_template_id_2fc26aab_scoped_true_staticRenderFns = [];

// CONCATENATED MODULE: ../components/lib/CustomDropdownWithSubMenu.vue?vue&type=template&id=2fc26aab&scoped=true&

// CONCATENATED MODULE: ./node_modules/babel-loader/lib??ref--2-0!./node_modules/@nuxt/components/dist/loader.js??ref--0-0!./node_modules/vue-loader/lib??vue-loader-options!../components/lib/CustomDropdownWithSubMenu.vue?vue&type=script&lang=js&
/* harmony default export */ var CustomDropdownWithSubMenuvue_type_script_lang_js_ = ({
  props: {
    items: {
      type: Array,
      required: true
    }
  },
  data() {
    return {
      isOpen: false,
      selectedSnippet: 1,
      selectedLibrary: 'XMLHttpRequest'
    };
  },
  computed: {
    selectedText() {
      if (!this.selectedSnippet || !this.items[this.selectedSnippet]) {
        return null;
      } else {
        if (!this.items[this.selectedSnippet].libraries) {
          return this.items[this.selectedSnippet].snippet;
        } else if (!this.selectedLibrary) {
          return null;
        } else {
          return this.items[this.selectedSnippet].snippet + ' / ' + this.items[this.selectedSnippet].libraries[this.selectedLibrary];
        }
      }
    }
  },
  methods: {
    handleItemClick(item, snippet, library = null) {
      if (item.libraries && !library) {
        this.selectedSnippet = snippet;
        this.selectedLibrary = null;
      } else {
        this.selectedSnippet = snippet;
        this.selectedLibrary = library;
        this.$emit('select', snippet, library);
        this.isOpen = false;
      }
    }
  }
});
// CONCATENATED MODULE: ../components/lib/CustomDropdownWithSubMenu.vue?vue&type=script&lang=js&
 /* harmony default export */ var lib_CustomDropdownWithSubMenuvue_type_script_lang_js_ = (CustomDropdownWithSubMenuvue_type_script_lang_js_); 
// CONCATENATED MODULE: ../components/lib/CustomDropdownWithSubMenu.vue



function CustomDropdownWithSubMenu_injectStyles (context) {
  
  var style0 = __webpack_require__(104)
if (style0.__inject__) style0.__inject__(context)

}

/* normalize component */

var CustomDropdownWithSubMenu_component = Object(componentNormalizer["a" /* default */])(
  lib_CustomDropdownWithSubMenuvue_type_script_lang_js_,
  CustomDropdownWithSubMenuvue_type_template_id_2fc26aab_scoped_true_render,
  CustomDropdownWithSubMenuvue_type_template_id_2fc26aab_scoped_true_staticRenderFns,
  false,
  CustomDropdownWithSubMenu_injectStyles,
  "2fc26aab",
  "641ebc78"
  
)

/* harmony default export */ var CustomDropdownWithSubMenu = (CustomDropdownWithSubMenu_component.exports);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/_CodeGenerator.js
/**
 *
 * generateHeaderFile(url) - генерация заголовка запроса;
 * generateFooterFile(url) - генерация завершения запроса;
 * generateMimeTypeHeader() - генерация заголовка типа контента;
 * generateHeaders() - генерация заголовков запроса;
 * generateQueryParams() - генерация параметров запроса в строке запроса;
 * generateJsonPostData() - генерация тела POST-запроса в формате JSON;
 * generateMultipartPostData() - генерация тела POST-запроса в формате multipart/form-data;
 * generateOtherPostData() - генерация тела POST-запроса в других форматах;
 * generateCookie() - генерация заголовка Cookie.
 *
 */
class _CodeGenerator_CodeGenerator {
  constructor(data) {
    this.baseUrl = void 0;
    this.url = void 0;
    this.method = void 0;
    this.params = void 0;
    this.mimeType = void 0;
    this.baseUrl = data.baseUrl;
    this.url = data.url;
    this.method = data.method;
    this.params = data.params;
    this.mimeType = data.mimeType;
  }
  generateCode() {
    let url = this.url;
    this.params.filter(param => param.in === 'path').forEach(param => {
      url = url.replace(`{${param.name}}`, param.value);
    });
    let fetchCode = this.generateHeaderFile(url);
    if (this.hasCookieParams()) {
      fetchCode += this.generateCookie();
    }
    if (this.hasMimeType()) {
      fetchCode += this.generateMimeTypeHeader();
    }
    if (this.hasHeadersParams()) {
      fetchCode += this.generateHeaders();
    }
    if (this.hasQueryParams()) {
      fetchCode += this.generateQueryParams();
    } else if (this.hasPostDataParams()) {
      if (this.mimeType === 'application/json') {
        fetchCode += this.generateJsonPostData();
      } else if (this.mimeType === 'multipart/form-data') {
        fetchCode += this.generateMultipartPostData();
      } else {
        fetchCode += this.generateOtherPostData();
      }
    }
    fetchCode += this.generateFooterFile(url);
    return fetchCode;
  }
  hasMimeType() {
    return !!this.mimeType;
  }
  hasHeadersParams() {
    return this.params.some(param => param.in === 'headers');
  }
  hasQueryParams() {
    return this.params.some(param => param.in === 'query');
  }
  hasPostDataParams() {
    return this.params.some(param => param.in === 'postData');
  }
  hasCookieParams() {
    return this.params.some(param => param.in === 'cookie');
  }
  getHost() {
    return this.params.some(param => param.in === 'cookie');
  }
  generateHeaderFile(url) {}
  generateFooterFile(url) {}
  generateMimeTypeHeader() {}
  generateHeaders() {}
  generateQueryParams() {}
  generateJsonPostData() {}
  generateMultipartPostData() {}
  generateOtherPostData() {}
  generateCookie() {}
}
/* harmony default export */ var _CodeGenerator = (_CodeGenerator_CodeGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/JavaScriptXMLHttpRequestGenerator.js

class JavaScriptXMLHttpRequestGenerator_JavaScriptXMLHttpRequestGenerator extends _CodeGenerator {
  generateCookie() {
    let cookieHeader = '';
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      cookieHeader += `${param.value}; `;
    });
    if (cookieHeader !== '') {
      cookieHeader = `xhr.setRequestHeader('Cookie', '${cookieHeader}');\n`;
    }
    return cookieHeader;
  }
  generateHeaderFile(url) {
    let queryParams = '';
    this.params.filter(param => param.in === 'query').forEach(param => {
      queryParams += `${param.name}=${param.value}&`;
    });
    if (queryParams !== '') {
      return `var xhr = new XMLHttpRequest();\nxhr.open('${this.method}', '${this.baseUrl}${this.url}?${queryParams.slice(0, -1)}');\n`;
    } else {
      return `var xhr = new XMLHttpRequest();\nxhr.open('${this.method}', '${this.baseUrl}${url}');\n`;
    }
  }
  generateFooterFile(url) {
    return `xhr.send();\n`;
  }
  generateMimeTypeHeader() {
    if (this.mimeType === 'application/json') {
      return `xhr.setRequestHeader('Content-Type', 'application/json');\n`;
    } else if (this.mimeType === 'multipart/form-data') {
      return 'Not supported in XmlHttpRequest';
    } else {
      return '';
    }
  }
  generateHeaders() {
    let headers = '';
    this.params.filter(param => param.in === 'headers').forEach(param => {
      headers += `xhr.setRequestHeader('${param.name}', '${param.value}');\n`;
    });
    return headers;
  }
  generateQueryParams() {
    return '';
  }
  generateJsonPostData() {
    let postData = JSON.stringify(this.params.filter(param => param.in === 'postData').reduce((obj, param) => {
      obj[param.name] = param.value;
      return obj;
    }, {}));
    return `xhr.send('${postData}');\n`;
  }
  generateMultipartPostData() {
    // Not supported in XmlHttpRequest
    return 'Not supported in XmlHttpRequest';
  }
  generateOtherPostData() {
    let postData = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      postData += `${param.name}=${param.value}&`;
    });
    postData = postData.slice(0, -1);
    return `xhr.setRequestHeader('Content-Type', 'application/x-www-form-urlencoded');\nxhr.send('${postData}');\n`;
  }
}
/* harmony default export */ var Stippets_JavaScriptXMLHttpRequestGenerator = (JavaScriptXMLHttpRequestGenerator_JavaScriptXMLHttpRequestGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/JavaScriptAxiosGenerator.js

class JavaScriptAxiosGenerator_JavaScriptAxiosGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `axios.${this.method.toLowerCase()}('${this.baseUrl}${url}', `;
  }
  generateFooterFile(url) {
    return `)`;
  }
  generateMimeTypeHeader() {
    if (this.mimeType === 'application/json') {
      return `headers: {         'Content-Type': 'application/json'       }, `;
    } else if (this.mimeType === 'multipart/form-data') {
      return `headers: {         'Content-Type': 'multipart/form-data'       }, `;
    } else {
      return '';
    }
  }
  generateHeaders() {
    let headers = `headers: {`;
    this.params.filter(param => param.in === 'headers').forEach(param => {
      headers += `${param.name}: '${param.value}', `;
    });
    headers += `}, `;
    return headers;
  }
  generateQueryParams() {
    let queryParams = `params: {`;
    this.params.filter(param => param.in === 'query').forEach(param => {
      queryParams += `${param.name}: '${param.value}', `;
    });
    queryParams += `}, `;
    return queryParams;
  }
  generateJsonPostData() {
    let postData = `data: {`;
    this.params.filter(param => param.in === 'postData').forEach(param => {
      postData += `${param.name}: ${JSON.stringify(param.value)}, `;
    });
    postData += `}, `;
    return postData;
  }
  generateMultipartPostData() {
    let formData = `data: new FormData(), `;
    this.params.filter(param => param.in === 'postData').forEach(param => {
      formData += `formData.append('${param.name}', ${JSON.stringify(param.value)}); `;
    });
    return formData;
  }
  generateOtherPostData() {
    let postData = `data: {`;
    this.params.filter(param => param.in === 'postData').forEach(param => {
      postData += `${param.name}: '${param.value}', `;
    });
    postData += `}, `;
    return postData;
  }
  generateCookie() {
    let cookieHeader = '';
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      cookieHeader += `${param.value}; `;
    });
    if (cookieHeader !== '') {
      cookieHeader = `headers: { Cookie: '${cookieHeader}' }, `;
    }
    return cookieHeader;
  }
}
/* harmony default export */ var Stippets_JavaScriptAxiosGenerator = (JavaScriptAxiosGenerator_JavaScriptAxiosGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/JavaScriptFetchGenerator.js

class JavaScriptFetchGenerator_JavaScriptFetchGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `fetch('${this.baseUrl}${url}', {\n\tmethod: '${this.method}',\n`;
  }
  generateHeaders() {
    const headersParams = this.params.filter(param => param.in === 'headers');
    const headers = headersParams.reduce((acc, cur) => ({
      ...acc,
      [cur.name]: cur.value
    }), {});
    return `\theaders: ${JSON.stringify(headers)},\n`;
  }
  generateMimeTypeHeader() {
    return `\theaders: { 'Content-Type': '${this.mimeType}' },\n`;
  }
  generateJsonPostData() {
    const postDataParams = this.params.filter(param => param.in === 'postData');
    const postData = JSON.stringify(postDataParams.reduce((acc, cur) => ({
      ...acc,
      [cur.name]: cur.value
    }), {}));
    return `\tbody: JSON.stringify(${postData}),\n`;
  }
  generateMultipartPostData() {
    const postDataParams = this.params.filter(param => param.in === 'postData');
    const formDataCode = postDataParams.map(param => ['${param.name}', '${param.value}']).join(',');
    return `\tbody: new FormData([${formDataCode}]),\n`;
  }
  generateOtherPostData() {
    const postDataParams = this.params.filter(param => param.in === 'postData');
    const postData = postDataParams.map(param => `${param.name}=${param.value}`).join('&');
    return `\tbody: '${postData}'\n`;
  }
  generateQueryParams() {
    const queryParams = this.params.filter(param => param.in === 'query');
    const queryString = queryParams.map(param => `${param.name}=${param.value}`).join('&');
    return `\tbody: '${queryString}'\n`;
  }
  generateFooterFile() {
    return `})`;
  }
  generateCookie() {
    let cookieHeader = '';
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      cookieHeader += `${param.value}; `;
    });
    if (cookieHeader !== '') {
      cookieHeader = `\theaders: { Cookie: '${cookieHeader}' },\n`;
    }
    return cookieHeader;
  }
}
/* harmony default export */ var Stippets_JavaScriptFetchGenerator = (JavaScriptFetchGenerator_JavaScriptFetchGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/JavaScriptJQueryGenerator.js

class JavaScriptJQueryGenerator_JavaScriptJQueryGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `$.ajax({\n       url: '${this.baseUrl}${url}',\n       type: '${this.method}',\n     `;
  }
  generateFooterFile(url) {
    return `});`;
  }
  generateMimeTypeHeader() {
    if (this.mimeType === 'application/json') {
      return `contentType: 'application/json', \n`;
    } else if (this.mimeType === 'multipart/form-data') {
      return `contentType: false,\n       processData: false, \n`;
    } else {
      return '';
    }
  }
  generateHeaders() {
    let headers = `headers: {\n`;
    this.params.filter(param => param.in === 'headers').forEach(param => {
      headers += `${param.name}: '${param.value}', \n`;
    });
    headers += `}, \n`;
    return headers;
  }
  generateQueryParams() {
    let queryParams = `data: {\n`;
    this.params.filter(param => param.in === 'query').forEach(param => {
      queryParams += `${param.name}: '${param.value}', `;
    });
    queryParams += `}, \n`;
    return queryParams;
  }
  generateJsonPostData() {
    let postData = `data: JSON.stringify({\n`;
    this.params.filter(param => param.in === 'postData').forEach(param => {
      postData += `${param.name}: ${JSON.stringify(param.value)}, \n`;
    });
    postData += `}), \n`;
    return postData;
  }
  generateMultipartPostData() {
    let formData = `data: new FormData(), \n`;
    this.params.filter(param => param.in === 'postData').forEach(param => {
      formData += `formData.append('${param.name}', ${JSON.stringify(param.value)}); \n`;
    });
    return formData;
  }
  generateOtherPostData() {
    let postData = `data: {\n`;
    this.params.filter(param => param.in === 'postData').forEach(param => {
      postData += `${param.name}: '${param.value}', \n`;
    });
    postData += `}, \n`;
    return postData;
  }
  generateCookie() {
    let cookieHeader = '';
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      cookieHeader += `${param.value}; \n`;
    });
    if (cookieHeader !== '') {
      cookieHeader = `headers: { Cookie: '${cookieHeader}' }, \n`;
    }
    return cookieHeader;
  }
}
/* harmony default export */ var Stippets_JavaScriptJQueryGenerator = (JavaScriptJQueryGenerator_JavaScriptJQueryGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/ShellCurlGenerator.js

class ShellCurlGenerator_ShellCurlGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `curl -X ${this.method} "${this.baseUrl}${url}"`;
  }
  generateFooterFile(url) {
    return '\n';
  }
  generateMimeTypeHeader() {
    return `-H "Content-Type: ${this.mimeType}" `;
  }
  generateHeaders() {
    let headersCode = '';
    this.params.filter(param => param.in === 'headers').forEach(param => {
      headersCode += `-H "${param.name}: ${param.value}" `;
    });
    return headersCode;
  }
  generateQueryParams() {
    let queryParamsCode = '';
    this.params.filter(param => param.in === 'query').forEach(param => {
      queryParamsCode += `${param.name}=${param.value}&`;
    });
    return `-G -d "${queryParamsCode.slice(0, -1)}" `;
  }
  generateJsonPostData() {
    let postDataCode = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      postDataCode += `"${param.name}": "${param.value}", `;
    });
    postDataCode = postDataCode.slice(0, -2);
    return `-d '{${postDataCode}}' `;
  }
  generateMultipartPostData() {
    let postDataCode = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      if (param.type === 'file') {
        postDataCode += `-F "${param.name}=@${param.path}" `;
      } else {
        postDataCode += `-F "${param.name}=${param.value}" `;
      }
    });
    return postDataCode;
  }
  generateOtherPostData() {
    let postDataCode = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      postDataCode += `${param.name}=${param.value}&`;
    });
    return `-d "${postDataCode.slice(0, -1)}" `;
  }
  generateCookie() {
    const cookieParams = this.params.filter(param => param.in === 'cookie');
    let cookieCode = '';
    cookieParams.forEach(param => {
      cookieCode += `-H "Cookie: ${param.name}=${param.value};" `;
    });
    return cookieCode;
  }
}
/* harmony default export */ var Stippets_ShellCurlGenerator = (ShellCurlGenerator_ShellCurlGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/ShellHttpieGenerator.js

class ShellHttpieGenerator_ShellHttpieGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `http ${this.method} ${this.baseUrl}${url}`;
  }
  generateFooterFile(url) {
    return '';
  }
  generateMimeTypeHeader() {
    if (this.mimeType === 'application/json') {
      return '--json';
    } else if (this.mimeType === 'multipart/form-data') {
      return '--form';
    } else {
      return '';
    }
  }
  generateHeaders() {
    const headers = this.params.filter(param => param.in === 'headers');
    return headers.map(header => `${header.name}:${header.value}`).join(' ');
  }
  generateQueryParams() {
    const queryParams = this.params.filter(param => param.in === 'query');
    return queryParams.map(queryParam => `${queryParam.name}=${queryParam.value}`).join(' ');
  }
  generateJsonPostData() {
    const postData = this.params.filter(param => param.in === 'postData');
    const jsonObj = postData.reduce((acc, curr) => {
      acc[curr.name] = curr.value;
      return acc;
    }, {});
    return JSON.stringify(jsonObj);
  }
  generateMultipartPostData() {
    const postData = this.params.filter(param => param.in === 'postData');
    const files = postData.filter(param => param.type === 'file');
    const formData = postData.filter(param => param.type !== 'file');
    let result = '';
    if (files.length > 0) {
      result += files.map(file => `${file.name}=@${file.path}`).join(' ');
    }
    if (formData.length > 0) {
      result += formData.map(data => `${data.name}=${data.value}`).join(' ');
    }
    return result;
  }
  generateOtherPostData() {
    const postData = this.params.filter(param => param.in === 'postData');
    const data = postData.reduce((acc, curr) => {
      acc[curr.name] = curr.value;
      return acc;
    }, {});
    return Object.keys(data).map(key => `${key}=${data[key]}`).join(' ');
  }
  generateCookie() {
    const cookies = this.params.filter(param => param.in === 'cookie');
    return `Cookie:${cookies.map(cookie => `${cookie.name}=${cookie.value}`).join(';')}`;
  }
}
/* harmony default export */ var Stippets_ShellHttpieGenerator = (ShellHttpieGenerator_ShellHttpieGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/ShellWgetGenerator.js

class ShellWgetGenerator_ShellWgetGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `wget --header='Accept: application/json' --no-check-certificate --quiet --output-document=- '${this.baseUrl}${url}'`;
  }
  generateFooterFile(url) {
    return '';
  }
  generateMimeTypeHeader() {
    if (this.mimeType === 'application/json') {
      return ` --header='Content-Type: application/json'`;
    } else if (this.mimeType === 'multipart/form-data') {
      return ` --header='Content-Type: multipart/form-data'`;
    } else {
      return ` --header='Content-Type: ${this.mimeType}'`;
    }
  }
  generateHeaders() {
    return this.params.filter(param => param.in === 'headers').map(param => ` --header='${param.name}: ${param.value}'`).join('');
  }
  generateQueryParams() {
    return this.params.filter(param => param.in === 'query').map(param => ` '${param.name}=${param.value}'`).join('');
  }
  generateJsonPostData() {
    return ` --post-data='${JSON.stringify(this.params.find(param => param.in === 'postData'))}'`;
  }
  generateMultipartPostData() {
    const postDataParam = this.params.find(param => param.in === 'postData');
    const fileParam = this.params.find(param => param.type === 'file');
    let postData = '';
    if (postDataParam) {
      for (const [key, value] of Object.entries(postDataParam.value)) {
        postData += `--form '${key}=${value}' `;
      }
    }
    if (fileParam) {
      postData += ` --form '${fileParam.name}=@${fileParam.path}'`;
    }
    return postData;
  }
  generateOtherPostData() {
    return ` --post-data='${this.params.find(param => param.in === 'postData').value}'`;
  }
  generateCookie() {
    return this.params.filter(param => param.in === 'cookie').map(param => ` --header='Cookie: ${param.name}=${param.value}'`).join('');
  }
  generateFilePath() {
    const fileParam = this.params.find(param => param.type === 'file');
    return fileParam ? fileParam.path : '';
  }
  generateWgetCommand() {
    let url = this.url;
    this.params.filter(param => param.in === 'path').forEach(param => {
      url = url.replace(`{${param.name}}`, param.value);
    });
    let command = this.generateHeaderFile(url);
    if (this.hasCookieParams()) {
      command += this.generateCookie();
    }
    if (this.hasMimeType()) {
      command += this.generateMimeTypeHeader();
    }
    if (this.hasHeadersParams()) {
      command += this.generateHeaders();
    }
    if (this.hasQueryParams()) {
      command += this.generateQueryParams();
    } else if (this.hasPostDataParams()) {
      if (this.mimeType === 'application/json') {
        command += this.generateJsonPostData();
      } else if (this.mimeType === 'multipart/form-data') {
        command += this.generateMultipartPostData();
      } else {
        command += this.generateOtherPostData();
      }
    }
    command += this.generateFooterFile(this.url);
    return command;
  }
  generateCode() {
    const command = this.generateWgetCommand();
    const filePath = this.generateFilePath();
    if (filePath) {
      return `${command} -O ${filePath}`;
    } else {
      return command;
    }
  }
}
/* harmony default export */ var Stippets_ShellWgetGenerator = (ShellWgetGenerator_ShellWgetGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/Python3Generator.js

class Python3Generator_Python3Generator extends _CodeGenerator {
  generateHeaderFile(url) {
    let code = `import http.client\n\n`;
    code += `conn = http.client.HTTPConnection('${this.getHost()}')\n`;
    code += `conn.request('${this.method}', '${url}'`;
    return code;
  }
  generateFooterFile(url) {
    let code = `\nresponse = conn.getresponse()\n`;
    code += `print(response.read().decode())\n`;
    code += `conn.close()\n`;
    return code;
  }
  generateMimeTypeHeader() {
    let code = `\nheaders = {\n`;
    code += `'Content-type': '${this.mimeType}'\n`;
    code += `}\n`;
    code += `conn.request_headers.update(headers)\n`;
    return code;
  }
  generateHeaders() {
    let code = `\nheaders = {\n`;
    this.params.filter(param => param.in === 'headers').forEach(param => {
      code += `'${param.name}': '${param.value}',\n`;
    });
    code += `}\n`;
    code += `conn.request_headers.update(headers)\n`;
    return code;
  }
  generateQueryParams() {
    let code = `\nquery_params = {\n`;
    this.params.filter(param => param.in === 'query').forEach(param => {
      code += `'${param.name}': '${param.value}',\n`;
    });
    code += `}\n`;
    code += `query_string = http.client.urlencode(query_params)\n`;
    code += `conn.request_url += '?' + query_string\n`;
    return code;
  }
  generateJsonPostData() {
    let code = `\nimport json\n`;
    code += `payload = {\n`;
    this.params.filter(param => param.in === 'postData').forEach(param => {
      code += `'${param.name}': '${param.value}',\n`;
    });
    code += `}\n`;
    code += `payload_json = json.dumps(payload)\n`;
    code += `conn.request_body = payload_json.encode()\n`;
    return code;
  }
  generateMultipartPostData() {
    let code = `\nimport os\n`;
    code += `import http.client\n`;
    code += `import mimetypes\n`;
    code += `boundary = 'boundary'\n`;
    code += `headers = {\n`;
    code += `'Content-Type': 'multipart/form-data; boundary=' + boundary\n`;
    code += `}\n`;
    code += `conn.request_headers.update(headers)\n`;
    code += `payload = b''\n`;
    this.params.filter(param => param.in === 'postData').forEach(param => {
      if (param.type === 'file' && param.path) {
        const filePath = param.path;
        const contentType = 'application/octet-stream';
        code += `payload += b'--' + boundary.encode() + b'\\r\\n'\n`;
        code += `payload += f'Content-Disposition: form-data; name="${param.name}"; filename="${param.name}"\\r\\n'.encode()\n`;
        code += `payload += f'Content-Type: ${contentType}\\r\\n\\r\\n'.encode()\n`;
        code += `with open('${filePath}', 'rb') as f:\n`;
        code += `    payload += f.read()\n`;
        code += `payload += b'\\r\\n'\n`;
      } else {
        code += `payload += b'--' + boundary.encode() + b'\\r\\n'\n`;
        code += `payload += f'Content-Disposition: form-data; name="${param.name}"\\r\\n\\r\\n'.encode()\n`;
        code += `payload += '${param.value}'.encode()\n`;
        code += `payload += b'\\r\\n'\n`;
      }
    });
    code += `payload += b'--' + boundary.encode() + b'--\\r\\n'\n`;
    code += `conn.request_body = payload\n`;
    return code;
  }
  generateOtherPostData() {
    let code = `\nimport http.client\n`;
    code += `payload = ''\n`;
    this.params.filter(param => param.in === 'postData').forEach(param => {
      code += `payload += '${param.name}=${param.value}&'\n`;
    });
    code += `payload = payload[:-1]\n`;
    code += `conn.request_body = payload.encode()\n`;
    return code;
  }
  generateCookie() {
    let code = `\ncookies = {\n`;
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      code += `'${param.name}': '${param.value}',\n`;
    });
    code += `}\n`;
    code += `cookie_string = http.client.cookies.format_cookies(cookies)\n`;
    code += `conn.request_headers['Cookie'] = cookie_string\n`;
    return code;
  }
}
/* harmony default export */ var Stippets_Python3Generator = (Python3Generator_Python3Generator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/PythonRequestsGenerator.js

class PythonRequestsGenerator_PythonRequestsGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    let code = `import http.client\n`;
    code += `conn = http.client.HTTPSConnection('${this.baseUrl}')\n`;
    code += `conn.request('${this.method}', '${url}', `;
    return code;
  }
  generateFooterFile(url) {
    let code = `response = conn.getresponse()\n`;
    code += `print(response.read().decode())\n`;
    code += `conn.close()\n`;
    return code;
  }
  generateMimeTypeHeader() {
    let code = `\nheaders = {\n`;
    code += `   'Content-type': '${this.mimeType}'\n`;
    code += `}\n`;
    code += `conn.request_headers.update(headers)\n`;
    return code;
  }
  generateHeaders() {
    let code = `\nheaders = {\n`;
    this.params.filter(param => param.in === 'headers').forEach(param => {
      code += `   '${param.name}': '${param.value}',\n`;
    });
    code += `}\n`;
    code += `conn.request_headers.update(headers)\n`;
    return code;
  }
  generateQueryParams() {
    let code = `\nurl += '?'\n`;
    this.params.filter(param => param.in === 'query').forEach((param, index) => {
      if (index > 0) {
        code += `url += '&'`;
      }
      code += `url += '${param.name}=${param.value}'\n`;
    });
    return code;
  }
  generateJsonPostData() {
    let code = `\nimport json\n`;
    code += `payload = json.dumps({\n`;
    this.params.filter(param => param.in === 'postData').forEach((param, index) => {
      if (index > 0) {
        code += ',\n';
      }
      code += `'${param.name}': '${param.value}'`;
    });
    code += `})\n`;
    code += `conn.request_body = payload.encode()\n`;
    return code;
  }
  generateMultipartPostData() {
    let code = `\nimport requests\n`;
    code += `files = {\n`;
    this.params.filter(param => param.in === 'postData' && param.type === 'file').forEach(param => {
      code += `'${param.name}': ('${param.path}', open('${param.path}', 'rb'), 'application/octet-stream')`;
    });
    code += `}\n`;
    code += `response = requests.post('${this.baseUrl}${this.url}', files=files)\n`;
    return code;
  }
  generateOtherPostData() {
    let code = `\nimport http.client\n`;
    code += `payload = ''\n`;
    this.params.filter(param => param.in === 'postData').forEach(param => {
      code += `payload += '${param.name}=${param.value}&'\n`;
    });
    code += `payload = payload[:-1]\n`;
    code += `conn.request_body = payload.encode()\n`;
    return code;
  }
  generateCookie() {
    let code = `\ncookies = {\n`;
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      code += `'${param.name}': '${param.value}',\n`;
    });
    code += `}\n`;
    code += `cookie_string = http.client.cookies.format_cookies(cookies)\n`;
    code += `conn.request_headers['Cookie'] = cookie_string\n`;
    return code;
  }
}
/* harmony default export */ var Stippets_PythonRequestsGenerator = (PythonRequestsGenerator_PythonRequestsGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/GoGenerator.js

class GoGenerator_GoGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    let code = `package main\n\nimport (\n\t"fmt"\n\t"net/http"\n\t"net/url"\n)\n\nfunc main() {\n`;
    code += `\turl, err := url.Parse("${this.baseUrl}${url}")\n`;
    code += `\tif err != nil {\n\t\tfmt.Println(err)\n\t\treturn\n\t}\n\n`;
    code += `\tclient := &http.Client{}\n`;
    code += `\treq, err := http.NewRequest("${this.method}", url.String(), nil)\n`;
    code += `\tif err != nil {\n\t\tfmt.Println(err)\n\t\treturn\n\t}\n\n`;
    return code;
  }
  generateFooterFile(url) {
    let code = `\tresp, err := client.Do(req)\n`;
    code += `\tif err != nil {\n\t\tfmt.Println(err)\n\t\treturn\n\t}\n\tdefer resp.Body.Close()\n\n`;
    code += `\t// TODO: process response\n}\n`;
    return code;
  }
  generateMimeTypeHeader() {
    let code = '';
    if (this.mimeType === 'application/json') {
      code += `\treq.Header.Set("Content-Type", "application/json")\n`;
    } else if (this.mimeType === 'multipart/form-data') {
      code += `\treq.Header.Set("Content-Type", "multipart/form-data")\n`;
    } else {
      code += `\treq.Header.Set("Content-Type", "application/x-www-form-urlencoded")\n`;
    }
    return code;
  }
  generateHeaders() {
    let code = '';
    this.params.filter(param => param.in === 'headers').forEach(param => {
      code += `\treq.Header.Set("${param.name}", "${param.value}")\n`;
    });
    return code;
  }
  generateQueryParams() {
    let code = '';
    const queryParams = this.params.filter(param => param.in === 'query');
    if (queryParams.length > 0) {
      code += `\tquery := url.Query()\n`;
      queryParams.forEach(param => {
        code += `\tquery.Add("${param.name}", "${param.value}")\n`;
      });
      code += `\turl.RawQuery = query.Encode()\n\n`;
    }
    return code;
  }
  generateJsonPostData() {
    let code = '';
    const postDataParams = this.params.filter(param => param.in === 'postData');
    if (postDataParams.length > 0) {
      code += `\t// TODO: implement JSON post data\n`;
    }
    return code;
  }
  generateMultipartPostData() {
    let code = '';
    const postDataParams = this.params.filter(param => param.in === 'postData');
    if (postDataParams.length > 0) {
      code += `\t// TODO: implement multipart post data\n`;
    }
    return code;
  }
  generateOtherPostData() {
    let code = '';
    const postDataParams = this.params.filter(param => param.in === 'postData');
    if (postDataParams.length > 0) {
      code += `\t// TODO: implement other post data\n`;
    }
    return code;
  }
  generateCookie() {
    let code = '';
    const cookieParams = this.params.filter(param => param.in === 'cookie');
    if (cookieParams.length > 0) {
      code += '\tcookies := []*http.Cookie{}\n';
      cookieParams.forEach(param => {
        code += `\tcookies = append(cookies, &http.Cookie{Name: "${param.name}", Value: "${param.value}"})\n`;
      });
      code += '\tcookieJar, _ := cookiejar.New(nil)\n';
      code += '\tcookieJar.SetCookies(url, cookies)\n';
      code += '\tclient.Jar = cookieJar\n\n';
    }
    return code;
  }
}
/* harmony default export */ var Stippets_GoGenerator = (GoGenerator_GoGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/CGenerator.js

class CGenerator_CGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `
#include <curl/curl.h>
#include <stdio.h>
#include <string.h>

int main(void) {
CURL *curl;
CURLcode res;
char *url = "${this.baseUrl}${url}";

curl = curl_easy_init();
if(curl) {
`;
  }
  generateFooterFile(url) {
    return `    curl_easy_setopt(curl, CURLOPT_URL, url);\n     res = curl_easy_perform(curl);\n     if(res != CURLE_OK)\n       fprintf(stderr, "curl_easy_perform() failed: %s\\n",\n               curl_easy_strerror(res));\n     curl_easy_cleanup(curl);\n   }\n   return 0;\n }\n`;
  }
  generateMimeTypeHeader() {
    if (this.mimeType === 'application/json') {
      return `    curl_easy_setopt(curl, CURLOPT_HTTPHEADER, ["Content-Type: application/json"]);`;
    } else if (this.mimeType === 'multipart/form-data') {
      return `
curl_mime *mime;
curl_mimepart *part;
struct curl_slist *headerlist = NULL;

mime = curl_mime_init(curl);

part = curl_mime_addpart(mime);
curl_mime_name(part, "field1");
curl_mime_data(part, "value1", CURL_ZERO_TERMINATED);

part = curl_mime_addpart(mime);
curl_mime_name(part, "field2");
curl_mime_filedata(part, "${this.params.find(param => param.type === 'file').path}");

headerlist = curl_slist_append(headerlist, "Content-Type: multipart/form-data");
curl_easy_setopt(curl, CURLOPT_HTTPHEADER, headerlist);
curl_easy_setopt(curl, CURLOPT_MIMEPOST, mime);
;     } else {       return
curl_easy_setopt(curl, CURLOPT_HTTPHEADER, ["Content-Type: ${this.mimeType}"]);
`;
    }
  }
  generateHeaders() {
    const headers = this.params.filter(param => param.in === 'headers');
    const headerStrings = headers.map(header => "${header.name}: ${header.value}");
    return `    curl_easy_setopt(curl, CURLOPT_HTTPHEADER, [${headerStrings.join(', ')}]);`;
  }
  generateQueryParams() {
    const queryParams = this.params.filter(param => param.in === 'query');
    const queryStrings = queryParams.map(param => `${param.name}=${param.value}`);
    const queryString = queryStrings.join('&');
    return `    char *query = "${queryString}";\n     char *urlWithQuery = malloc(strlen(url) + strlen(query) + 2);\n     strcpy(urlWithQuery, url);\n     strcat(urlWithQuery, "?");\n     strcat(urlWithQuery, query);\n     curl_easy_setopt(curl, CURLOPT_URL, urlWithQuery);\n`;
  }
  generateJsonPostData() {
    const postData = this.params.find(param => param.in === 'postData').value;
    return `    curl_easy_setopt(curl, CURLOPT_POSTFIELDS, "${postData}");\n`;
  }
  generateMultipartPostData() {
    return this.generateMimeTypeHeader();
  }
  generateOtherPostData() {
    const postData = this.params.find(param => param.in === 'postData').value;
    return `    curl_easy_setopt(curl, CURLOPT_POSTFIELDS, "${postData}");`;
  }
  generateCookie() {
    const cookieParams = this.params.filter(param => param.in === 'cookie');
    const cookieStrings = cookieParams.map(param => `${param.name}=${param.value}`);
    const cookieString = cookieStrings.join('; ');
    return `    curl_easy_setopt(curl, CURLOPT_COOKIE, "${cookieString}");`;
  }
}
/* harmony default export */ var Stippets_CGenerator = (CGenerator_CGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/OcamlGenerator.js

class OcamlGenerator_OcamlGenerator extends _CodeGenerator {
  constructor(data) {
    super(data);
  }
  generateHeaderFile(url) {
    return `let url = "${this.baseUrl}${url}" in\n`;
  }
  generateFooterFile() {
    return `;;`;
  }
  generateMimeTypeHeader() {
    if (this.mimeType === 'application/json') {
      return `let headers = [("Content-Type", "application/json")] in\n`;
    } else if (this.mimeType === 'multipart/form-data') {
      return `let headers = [("Content-Type", "multipart/form-data")] in\n`;
    } else {
      return `let headers = [("Content-Type", "application/x-www-form-urlencoded")] in\n`;
    }
  }
  generateHeaders() {
    const headers = this.params.filter(param => param.in === 'headers').map(param => `("${param.name}", "${param.value}")`).join('; ');
    return `let headers = [${headers}] in\n`;
  }
  generateQueryParams() {
    const params = this.params.filter(param => param.in === 'query').map(param => `("${param.name}", "${param.value}")`).join('; ');
    return `let params = [${params}] in\n`;
  }
  generateJsonPostData() {
    const data = this.params.find(param => param.in === 'postData').value;
    return `let body = ${JSON.stringify(data)} in\n`;
  }
  generateMultipartPostData() {
    const data = this.params.find(param => param.in === 'postData');
    const name = data.name;
    return `let body = [("${name}", "file")] in\n`;
  }
  generateOtherPostData() {
    const data = this.params.find(param => param.in === 'postData').value;
    return `let body = "${data}" in\n`;
  }
  generateCookie() {
    const cookies = this.params.filter(param => param.in === 'cookie').map(param => `("${param.name}", "${param.value}")`).join('; ');
    return `let cookies = [${cookies}] in\n`;
  }
}
/* harmony default export */ var Stippets_OcamlGenerator = (OcamlGenerator_OcamlGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/CSharpHttpclientGenerator.js

class CSharpHttpclientGenerator_CSharpHttpClientGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `using System;
    using System.Net.Http;

    namespace MyNamespace
    {
        class MyClass
        {
            static HttpClient client = new HttpClient();

            static void Main()
            {
                var url = "${this.baseUrl}${url}";
                `;
  }
  generateFooterFile(url) {
    return `
            Console.WriteLine(responseString);
            }
        }
    }`;
  }
  generateMimeTypeHeader() {
    if (this.mimeType === "application/json") {
      return `client.DefaultRequestHeaders.Accept.Add(new MediaTypeWithQualityHeaderValue("application/json"));`;
    } else if (this.mimeType === "multipart/form-data") {
      return `client.DefaultRequestHeaders.Accept.Add(new MediaTypeWithQualityHeaderValue("multipart/form-data"));`;
    } else {
      return ``;
    }
  }
  generateHeaders() {
    let headers = "";
    this.params.filter(param => param.in === "headers").forEach(param => {
      headers += `client.DefaultRequestHeaders.Add("${param.name}", "${param.value}");\n`;
    });
    return headers;
  }
  generateQueryParams() {
    let query = "";
    this.params.filter(param => param.in === "query").forEach(param => {
      query += `${param.name}=${param.value}&`;
    });
    if (query !== "") {
      query = "?" + query.slice(0, -1);
    }
    return `url += "${query}";`;
  }
  generateJsonPostData() {
    return `var postData = new StringContent("${JSON.stringify(this.params.filter(param => param.in === "postData")[0].value)}", System.Text.Encoding.UTF8, "application/json");`;
  }
  generateMultipartPostData() {
    return `var formData = new MultipartFormDataContent();
    `;
  }
  generateOtherPostData() {
    return `var postData = new StringContent("${this.params.filter(param => param.in === "postData")[0].value}", System.Text.Encoding.UTF8, "${this.mimeType}");`;
  }
  generateCookie() {
    let cookies = "";
    this.params.filter(param => param.in === "cookie").forEach(param => {
      cookies += `${param.name}=${param.value}; `;
    });
    return `client.DefaultRequestHeaders.Add("Cookie", "${cookies.trim()}");`;
  }
}
/* harmony default export */ var CSharpHttpclientGenerator = (CSharpHttpclientGenerator_CSharpHttpClientGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/CSharpRestSharpGenerator.js

class CSharpRestSharpGenerator_CSharpRestSharpGenerator extends _CodeGenerator {
  constructor(data) {
    super(data);
  }
  generateHeaderFile(url) {
    return `var client = new RestSharp.RestClient("${this.baseUrl}");
var request = new RestSharp.RestRequest("${url}", RestSharp.Method.${this.method});
`;
  }
  generateFooterFile(url) {
    return `var response = client.Execute(request);
Console.WriteLine(response.Content);`;
  }
  generateMimeTypeHeader() {
    return `request.AddParameter("${this.mimeType}", body, RestSharp.ParameterType.RequestBody);`;
  }
  generateHeaders() {
    let headers = '';
    this.params.filter(param => param.in === 'headers').forEach(param => {
      headers += `request.AddHeader("${param.name}", "${param.value}");
`;
    });
    return headers;
  }
  generateQueryParams() {
    let queryParams = '';
    this.params.filter(param => param.in === 'query').forEach(param => {
      queryParams += `request.AddParameter("${param.name}", "${param.value}");
`;
    });
    return queryParams;
  }
  generateJsonPostData() {
    return `var body = JsonConvert.SerializeObject({${this.params.filter(param => param.in === 'postData').map(param => `"${param.name}": "${param.value}"`).join(',')}});`;
  }
  generateMultipartPostData() {
    const params = this.params.filter(param => param.in === 'postData').forEach(param => `body.Add(new RestSharp.FormDataParameter("${param.name}", "${param.value}"`);
    return `var body = new RestSharp.MultipartFormDataContent();
${params}`;
  }
  generateOtherPostData() {
    return `var body = "${this.params.find(param => param.in === 'postData').value}";`;
  }
  generateCookie() {
    let cookies = '';
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      cookies += `request.AddCookie("${param.name}", "${param.value}");`;
    });
    return cookies;
  }
}
/* harmony default export */ var Stippets_CSharpRestSharpGenerator = (CSharpRestSharpGenerator_CSharpRestSharpGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/JavaAsyncHttpClientGenerator.js

class JavaAsyncHttpClientGenerator_JavaAsyncHttpClientGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `AsyncHttpClient asyncHttpClient = new DefaultAsyncHttpClient();
        BoundRequestBuilder requestBuilder = asyncHttpClient.prepare("${this.method}", "${this.baseUrl}${url}")`;
  }
  generateFooterFile(url) {
    return `ListenableFuture<Response> responseFuture = requestBuilder.execute();
        Response response = responseFuture.get();
        asyncHttpClient.close();
        System.out.println(response.getResponseBody());`;
  }
  generateMimeTypeHeader() {
    return `requestBuilder.addHeader("Content-Type", "${this.mimeType}");`;
  }
  generateHeaders() {
    let headers = '';
    this.params.forEach(param => {
      if (param.in === 'headers') {
        headers += `requestBuilder.addHeader("${param.name}", "${param.value}");\n`;
      }
    });
    return headers;
  }
  generateQueryParams() {
    let queryParams = '';
    this.params.forEach(param => {
      if (param.in === 'query') {
        queryParams += `requestBuilder.addQueryParam("${param.name}", "${param.value}");\n`;
      }
    });
    return queryParams;
  }
  generateJsonPostData() {
    let jsonData = '';
    this.params.forEach(param => {
      if (param.in === 'postData') {
        jsonData += `String json = "${param.value}";\n`;
        jsonData += `requestBuilder.setBody(json);\n`;
      }
    });
    return jsonData;
  }
  generateMultipartPostData() {
    const formData = this.params.filter(param => param.in === 'postData').reduce((formData, param) => {
      if (param.type === 'file') {
        // If the parameter is a file, add its contents as a buffer to the form data
        formData.push(`new FilePart("${param.name}", new File("${param.path}"), "${param.path}")`);
      } else {
        // If the parameter is not a file, add its value as a string to the form data
        formData.push(`new StringPart("${param.name}", "${param.value}")`);
      }
      return formData;
    }, []);
    let multipartData = `requestBuilder.setBody(new MultipartBodyBuilder()`;
    formData.forEach(data => {
      multipartData += `\n.addPart(${data})`;
    });
    multipartData += `\n.build());\n`;
    return multipartData;
  }
  generateOtherPostData() {
    let postData = '';
    this.params.forEach(param => {
      if (param.in === 'postData') {
        postData += `String postData = "${param.value}";\n`;
        postData += `requestBuilder.setBody(postData);\n`;
      }
    });
    return postData;
  }
  generateCookie() {
    let cookieData = '';
    this.params.forEach(param => {
      if (param.in === 'cookie') {
        cookieData += `requestBuilder.addCookie(new Cookie("${param.name}", "${param.value}"));\n`;
      }
    });
    return cookieData;
  }
}
/* harmony default export */ var Stippets_JavaAsyncHttpClientGenerator = (JavaAsyncHttpClientGenerator_JavaAsyncHttpClientGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/JavaNetHttpClientGenerator.js

class JavaNetHttpClientGenerator_JavaNetHttpClientGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `HttpClient client = HttpClient.newHttpClient();
HttpRequest request = HttpRequest.newBuilder()
        .uri(URI.create("${this.baseUrl}${url}"))
        .method("${this.method}", HttpRequest.BodyPublishers.noBody())`;
  }
  generateFooterFile(url) {
    return `HttpResponse<String> response = client.send(request, HttpResponse.BodyHandlers.ofString());
System.out.println(response.body());`;
  }
  generateMimeTypeHeader() {
    switch (this.mimeType) {
      case 'application/json':
        return `request = request.header("Content-Type", "application/json");`;
      case 'multipart/form-data':
        return `request = request.header("Content-Type", "multipart/form-data");`;
      case 'application/x-www-form-urlencoded':
        return `request = request.header("Content-Type", "application/x-www-form-urlencoded");`;
      default:
        return '';
    }
  }
  generateHeaders() {
    return this.params.filter(param => param.in === 'headers').map(param => `request = request.header("${param.name}", "${param.value}");`).join('\n');
  }
  generateQueryParams() {
    const queryParams = this.params.filter(param => param.in === 'query');
    if (queryParams.length === 0) {
      return '';
    }
    const queryStr = queryParams.map(param => `${param.name}=${param.value}`).join('&');
    return `request = request.uri(URI.create("${this.baseUrl}${this.url}?${queryStr}"));`;
  }
  generateJsonPostData() {
    const postData = this.params.find(param => param.in === 'postData');
    if (!postData) {
      return '';
    }
    return `String json = "${postData.value}";
request = request.method("${this.method}", HttpRequest.BodyPublishers.ofString(json));`;
  }
  generateMultipartPostData() {
    const postData = this.params.find(param => param.in === 'postData');
    if (!postData || postData.type !== 'file' || !postData.path) {
      return '';
    }
    return `Path path = Paths.get("${postData.path}");
String fileName = path.getFileName().toString();
String boundary = "*****";
String CRLF = "\\r\\n";

request = request.method("${this.method}", HttpRequest.BodyPublishers.ofMultipartData(
        new MultipartBodyPublisher(boundary,
                List.of(
                        new FilePart("file", fileName, path.toFile())
                )
        )
));`;
  }
  generateOtherPostData() {
    const postData = this.params.find(param => param.in === 'postData');
    if (!postData) {
      return '';
    }
    return `String data = "${postData.value}";
request = request.method("${this.method}", HttpRequest.BodyPublishers.ofString(data));`;
  }
  generateCookie() {
    return this.params.filter(param => param.in === 'cookie').map(param => `request = request.header("Cookie", "${param.name}=${param.value}");`).join('\n');
  }
}
/* harmony default export */ var Stippets_JavaNetHttpClientGenerator = (JavaNetHttpClientGenerator_JavaNetHttpClientGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/JavaOkHttpGenerator.js

const OKHTTP_IMPORTS = `import okhttp3.*;\nimport java.io.*;\n`;
class JavaOkHttpGenerator_JavaOkHttpGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    let code = OKHTTP_IMPORTS + `OkHttpClient client = new OkHttpClient();\n`;
    code += `HttpUrl.Builder urlBuilder = HttpUrl.parse("${this.baseUrl}" + "${url}").newBuilder();\n`;
    code += `Request.Builder requestBuilder = new Request.Builder().url(urlBuilder.build());\n`;
    code += `requestBuilder.method("${this.method}", null);\n`;
    return code;
  }
  generateFooterFile() {
    let code = `Response response = client.newCall(requestBuilder.build()).execute();\n`;
    code += `System.out.println(response.body().string());\n`;
    return code;
  }
  generateMimeTypeHeader() {
    return `requestBuilder.header("Content-Type", "${this.mimeType}");\n`;
  }
  generateHeaders() {
    let code = '';
    this.params.filter(param => param.in === 'headers').forEach(param => {
      code += `requestBuilder.header("${param.name}", "${param.value}");\n`;
    });
    return code;
  }
  generateQueryParams() {
    let code = '';
    this.params.filter(param => param.in === 'query').forEach(param => {
      code += `urlBuilder.addQueryParameter("${param.name}", "${param.value}");\n`;
    });
    return code;
  }
  generateJsonPostData() {
    let code = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      if (this.mimeType === 'application/json') {
        code += `String json = "${param.value}";\n`;
        code += `RequestBody body = RequestBody.create(MediaType.parse("application/json; charset=utf-8"), json);\n`;
        code += `requestBuilder.method("${this.method}", body);\n`;
      }
    });
    return code;
  }
  generateMultipartPostData() {
    let code = '';
    this.params.filter(param => param.in === 'postData' && param.type === 'file').forEach(param => {
      code += `RequestBody body = RequestBody.create(MediaType.parse("multipart/form-data"), ${param.name});\n`;
      code += `requestBuilder.method("${this.method}", new Request.Builder().url(urlBuilder.build()).post(body).build(), body);\n`;
      code += `client.newCall(requestBuilder.build()).execute().body().string();\n`;
    });
    return code;
  }
  generateOtherPostData() {
    let code = '';
    this.params.filter(param => param.in === 'postData' && param.type !== 'file').forEach(param => {
      code += `RequestBody body = RequestBody.create(MediaType.parse("${this.mimeType}"), "${param.value}");\n`;
      code += `requestBuilder.method("${this.method}", body);\n`;
    });
    return code;
  }
  generateCookie() {
    let code = '';
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      code += `requestBuilder.addHeader("Cookie", "${param.name}=${param.value}");\n`;
    });
    return code;
  }
}
/* harmony default export */ var Stippets_JavaOkHttpGenerator = (JavaOkHttpGenerator_JavaOkHttpGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/HttpUrlStringGenerator.js

class HttpUrlStringGenerator_HttpUrlStringGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    const headers = this.getHeaders();
    const cookie = this.getCookie();
    let headerFile = `${this.method} ${this.baseUrl}${this.url} HTTP/1.1\r\n` + `Host: ${this.getHost()}\r\n`;
    if (cookie) {
      headerFile += `Cookie: ${cookie}\r\n`;
    }
    for (let i in headers) {
      headerFile += `${i}: ${headers[i]}\r\n`;
    }
    if (this.hasPostDataParams()) {
      const postData = this.getPostData();
      if (postData) {
        headerFile += `Content-Type: ${this.mimeType}\r\n`;
        headerFile += `Content-Length: ${Buffer.byteLength(postData.toString())}\r\n`;
      }
    }
    headerFile += '\r\n';
    return headerFile;
  }
  generateFooterFile(url) {
    return '';
  }
  generateMimeTypeHeader() {
    return '';
  }
  generateHeaders() {
    const headers = this.getHeaders();
    let headersString = '';
    for (const header in headers) {
      headersString += `${header}: ${headers[header]}\r\n`;
    }
    return headersString;
  }
  generateQueryParams() {
    return '';
  }
  generateJsonPostData() {
    const postData = this.getPostData();
    return JSON.stringify(postData);
  }
  generateMultipartPostData() {
    const boundary = '----WebKitFormBoundary7MA4YWxkTrZu0gW';
    const formData = this.params.filter(param => param.in === 'postData').reduce((formData, param) => {
      formData.push(`--${boundary}\r\n` + `Content-Disposition: form-data; name="${param.name}"\r\n\r\n` + `${param.value}\r\n`);
      return formData;
    }, []);

    // Add the closing boundary to the form data
    formData.push(`--${boundary}--\r\n`);

    // Join the form data into a single string and return it
    return formData.join('');
  }
  generateOtherPostData() {
    const postData = this.getPostData();
    return this.serializeQueryParams(postData);
  }
  generateCookie() {
    return this.getCookie();
  }
  serializeQueryParams(params) {
    return Object.keys(params).map(key => `${encodeURIComponent(key)}=${encodeURIComponent(params[key])}`).join('&');
  }
  getHeaders() {
    const headers = {};
    this.params.filter(param => param.in === 'headers').forEach(param => {
      headers[param.name] = param.value;
    });
    return headers;
  }
  getPostData() {
    return this.params.filter(param => param.in === 'postData').reduce((postData, param) => {
      postData[param.name] = param.value;
      return postData;
    }, {});
  }
  getCookie() {
    return this.params.filter(param => param.in === 'cookie').map(param => `${encodeURIComponent(param.name)}=${encodeURIComponent(param.value)}`).join('; ');
  }
}
/* harmony default export */ var Stippets_HttpUrlStringGenerator = (HttpUrlStringGenerator_HttpUrlStringGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/JavaUnirestGenerator.js

class JavaUnirestGenerator_JavaUnirestGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `HttpResponse<String> response = Unirest.${this.method.toLowerCase()}("${this.baseUrl}${url}")\n`;
  }
  generateFooterFile(url) {
    return `HttpResponse<String> response = request.asString();\n`;
  }
  generateMimeTypeHeader() {
    if (this.mimeType === 'application/json') {
      return `header("Content-Type", "application/json")\n`;
    } else if (this.mimeType === 'multipart/form-data') {
      return `Not supported in Unirest\n`;
    } else {
      return '';
    }
  }
  generateHeaders() {
    let headers = '';
    this.params.filter(param => param.in === 'headers').forEach(param => {
      headers += `.header("${param.name}", "${param.value}")\n`;
    });
    return headers;
  }
  generateQueryParams() {
    let queryParams = '';
    this.params.filter(param => param.in === 'query').forEach(param => {
      queryParams += `.queryString("${param.name}", "${param.value}")\n`;
    });
    return queryParams;
  }
  generateJsonPostData() {
    let postData = JSON.stringify(this.params.filter(param => param.in === 'postData').reduce((obj, param) => {
      obj[param.name] = param.value;
      return obj;
    }, {}));
    return `.body("${postData}")\n`;
  }
  generateMultipartPostData() {
    return 'Not supported in Unirest\n';
  }
  generateOtherPostData() {
    let postData = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      postData += `${param.name}=${param.value}&`;
    });
    postData = postData.slice(0, -1);
    return `.body("${postData}")\n`;
  }
  generateCookie() {
    let cookieHeader = '';
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      cookieHeader += `${param.value}; \n`;
    });
    if (cookieHeader !== '') {
      cookieHeader = `.header("Cookie", "${cookieHeader}")\n`;
    }
    return cookieHeader;
  }
}
/* harmony default export */ var Stippets_JavaUnirestGenerator = (JavaUnirestGenerator_JavaUnirestGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/ClojureHttpGenerator.js

class ClojureHttpGenerator_ClojureGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `(require '[clj-http.client :as client])
(def url "${url}")\n`;
  }
  generateFooterFile(url) {
    return '';
  }
  generateMimeTypeHeader() {
    return '';
  }
  generateHeaders() {
    const headers = this.params.find(param => param.in === 'headers').value;
    const headerString = Object.entries(headers).map(([key, value]) => `("${key}" "${value}")`).join('\n');
    return `(def headers [${headerString}])\n`;
  }
  generateQueryParams() {
    const queryParams = this.params.filter(param => param.in === 'query').map(({
      value,
      name
    }) => `("${name}" "${value}")`).join('\n');
    return `(def query-params [${queryParams}])\n`;
  }
  generateJsonPostData() {
    const postData = this.params.find(param => param.in === 'postData').value;
    const postDataString = JSON.stringify(postData).replace(/"/g, '\\"').replace(/:/g, ' ').replace(/,/g, '\n');
    return `(def post-data "${postDataString}")\n`;
  }
  generateMultipartPostData() {
    // Not implemented
    return '';
  }
  generateOtherPostData() {
    const postData = this.params.find(param => param.in === 'postData').value;
    const postDataString = JSON.stringify(postData).replace(/"/g, '\\"').replace(/:/g, ' ').replace(/,/g, '\n');
    return `(def post-data "${postDataString}")\n`;
  }
  generateCookie() {
    const cookies = this.params.find(param => param.in === 'cookie').value;
    const cookieString = Object.entries(cookies).map(([key, value]) => `("${key}" "${value}")`).join('\n');
    return `(def cookies [${cookieString}])\n`;
  }
  generateCode() {
    let url = this.url;
    this.params.filter(param => param.in === 'path').forEach(param => {
      url = url.replace(`{${param.name}}`, param.value);
    });
    let clojureCode = this.generateHeaderFile(url);
    if (this.hasCookieParams()) {
      clojureCode += this.generateCookie();
    }
    if (this.hasHeadersParams()) {
      clojureCode += this.generateHeaders();
    }
    if (this.hasQueryParams()) {
      clojureCode += this.generateQueryParams();
    } else if (this.hasPostDataParams()) {
      if (this.mimeType === 'application/json') {
        clojureCode += this.generateJsonPostData();
      } else if (this.mimeType === 'multipart/form-data') {
        clojureCode += this.generateMultipartPostData();
      } else {
        clojureCode += this.generateOtherPostData();
      }
    }
    clojureCode += `(def response (client/${this.method.toLowerCase()} url`;
    if (this.hasHeadersParams()) {
      clojureCode += '\n {:headers headers}';
    }
    if (this.hasQueryParams()) {
      clojureCode += '\n {:query-params query-params}';
    }
    if (this.hasPostDataParams()) {
      clojureCode += '\n {:body post-data}';
    }
    if (this.hasCookieParams()) {
      clojureCode += '\n {:cookies cookies}';
    }
    clojureCode += '))\n';
    clojureCode += this.generateFooterFile(url);
    return clojureCode;
  }
}
/* harmony default export */ var ClojureHttpGenerator = (ClojureHttpGenerator_ClojureGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/KotlinGenerator.js

class KotlinGenerator_KotlinGenerator extends _CodeGenerator {
  constructor(data) {
    super(data);
  }
  generateHeaderFile(url) {
    return `
      val url = "${this.baseUrl}${url}"
      val request = HttpRequest(url = url, method = HttpMethod.${this.method})
    `;
  }
  generateFooterFile(url) {
    return `
      val response = client.execute(request)
      if (response.status.isSuccess()) {
          // handle successful response
      } else {
          // handle unsuccessful response
      }
    `;
  }
  generateMimeTypeHeader() {
    return `
      request.headers.append("Content-Type", "${this.mimeType}")
    `;
  }
  generateHeaders() {
    const headers = this.params.filter(param => param.in === 'headers');
    let headersCode = '';
    for (let i = 0; i < headers.length; i++) {
      const header = headers[i];
      headersCode += `
        request.headers.append("${header.name}", "${header.value}")
      `;
    }
    return headersCode;
  }
  generateQueryParams() {
    const queryParams = this.params.filter(param => param.in === 'query');
    let queryParamsCode = '';
    for (let i = 0; i < queryParams.length; i++) {
      const queryParam = queryParams[i];
      queryParamsCode += `
        request.parameters.append("${queryParam.name}", "${queryParam.value}")
      `;
    }
    return queryParamsCode;
  }
  generateJsonPostData() {
    const postDataParams = this.params.filter(param => param.in === 'postData');
    let postDataCode = '';
    for (let i = 0; i < postDataParams.length; i++) {
      const postDataParam = postDataParams[i];
      postDataCode += `
        val ${postDataParam.name} = ${postDataParam.value}
      `;
    }
    postDataCode += `
      val json = Json { ignoreUnknownKeys = true }
      request.body = TextContent(json.encodeToString(${postDataParams[0].name}), contentType = ContentType.Application.Json)
    `;
    return postDataCode;
  }
  generateMultipartPostData() {
    const postDataParams = this.params.filter(param => param.in === 'postData');
    let postDataCode = '';
    for (let i = 0; i < postDataParams.length; i++) {
      const postDataParam = postDataParams[i];
      if (postDataParam.type === 'file') {
        postDataCode += `
          val ${postDataParam.name}File = File("${postDataParam.path}")
          val ${postDataParam.name}Part = formData {
              append("${postDataParam.name}", ${postDataParam.name}File.readBytes(), Headers.build {
                  append(HttpHeaders.ContentDisposition, "filename=\${${postDataParam.name}File.name}")
              })
          }
        `;
      } else {
        postDataCode += `
          val ${postDataParam.name}Part = formData {
              append("${postDataParam.name}", "${postDataParam.value}")
          }
        `;
      }
    }
    postDataCode += `
      request.body = ${postDataParams.map(param => `${param.name}Part`).join(' + ')}
    `;
    return postDataCode;
  }
  generateOtherPostData() {
    const postDataParams = this.params.filter(param => param.in === 'postData');
    let postDataCode = '';
    for (let i = 0; i < postDataParams.length; i++) {
      const postDataParam = postDataParams[i];
      postDataCode += `
        val ${postDataParam.name} = "${postDataParam.value}"
      `;
    }
    postDataCode += `
      request.body = TextContent("${postDataParams.map(param => `\${${param.name}}`).join('&')}", contentType = ContentType.Application.FormUrlEncoded)
    `;
    return postDataCode;
  }
  generateCookie() {
    const cookieParams = this.params.filter(param => param.in === 'cookie');
    let cookieCode = '';
    for (let i = 0; i < cookieParams.length; i++) {
      const cookieParam = cookieParams[i];
      cookieCode += `
        request.cookies.append("${cookieParam.name}", "${cookieParam.value}")
      `;
    }
    return cookieCode;
  }
}
/* harmony default export */ var Stippets_KotlinGenerator = (KotlinGenerator_KotlinGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/PhpCurlGenerator.js

class PhpCurlGenerator_PhpCurlGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `<?php
$ch = curl_init();
curl_setopt($ch, CURLOPT_URL, '${this.baseUrl}${url}');
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
`;
  }
  generateFooterFile(url) {
    return `curl_close($ch);
?>`;
  }
  generateMimeTypeHeader() {
    if (this.mimeType === 'application/json') {
      return `curl_setopt($ch, CURLOPT_HTTPHEADER, array('Content-Type: application/json'));`;
    } else if (this.mimeType === 'multipart/form-data') {
      return `curl_setopt($ch, CURLOPT_HTTPHEADER, array('Content-Type: multipart/form-data'));`;
    } else {
      return '';
    }
  }
  generateHeaders() {
    const headers = this.params.filter(param => param.in === 'headers');
    if (headers.length) {
      const headerStr = headers.map(header => `${header.name}: ${header.value}`).join('\\r\\n');
      return `curl_setopt($ch, CURLOPT_HTTPHEADER, array('${headerStr}'));`;
    } else {
      return '';
    }
  }
  generateQueryParams() {
    const queryParams = this.params.filter(param => param.in === 'query');
    const queryStr = queryParams.map(param => `${param.name}=${param.value}`).join('&');
    return `curl_setopt($ch, CURLOPT_URL, '${this.baseUrl}${this.url}?${queryStr}');`;
  }
  generateJsonPostData() {
    const postData = this.params.find(param => param.in === 'postData');
    return `curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode(${postData.value}));`;
  }
  generateMultipartPostData() {
    const formData = this.params.filter(param => param.in === 'postData');
    const formDataStr = formData.map(param => `--boundary\\r\\nContent-Disposition: form-data; name="${param.name}"; filename="${param.path}"\\r\\nContent-Type: ${param.type}\\r\\n\\r\\n${param.value}\\r\\n`).join('');
    return `curl_setopt($ch, CURLOPT_POSTFIELDS, '${formDataStr}--boundary--\\r\\n');`;
  }
  generateOtherPostData() {
    const postData = this.params.find(param => param.in === 'postData');
    return `curl_setopt($ch, CURLOPT_POSTFIELDS, '${postData.value}');`;
  }
  generateCookie() {
    const cookieParams = this.params.filter(param => param.in === 'cookie');
    const cookieStr = cookieParams.map(param => `${param.name}=${param.value}`).join('; ');
    return `curl_setopt($ch, CURLOPT_COOKIE, '${cookieStr}');`;
  }
}
/* harmony default export */ var Stippets_PhpCurlGenerator = (PhpCurlGenerator_PhpCurlGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/PhpGuzzleGenerator.js

class PhpGuzzleGenerator_PhpGuzzleGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `<?php
require_once 'vendor/autoload.php';

$httpClient = new GuzzleHttp\\Client([
  'base_uri' => '${this.baseUrl}',
  'verify' => false,
]);

$requestOptions = [
  RequestOptions::${this.method},
  '${url}',
`;
  }
  generateFooterFile() {
    return `
];

$response = $httpClient->request(...$requestOptions);

echo $response->getBody();
`;
  }
  generateMimeTypeHeader() {
    return `  'headers' => [
    'Content-Type' => '${this.mimeType}',
  ],
`;
  }
  generateHeaders() {
    const headers = this.params.filter(param => param.in === 'headers');
    if (headers.length === 0) return '';
    return `  'headers' => [
${headers.map(param => `    '${param.name}' => '${param.value}',`).join('\n')}
  ],
`;
  }
  generateQueryParams() {
    const queryParams = this.params.filter(param => param.in === 'query');
    if (queryParams.length === 0) return '';
    return `  'query' => [
${queryParams.map(param => `    '${param.name}' => '${param.value}',`).join('\n')}
  ],
`;
  }
  generateJsonPostData() {
    const postData = this.params.find(param => param.in === 'postData');
    if (!postData) return '';
    return `  'json' => ${postData.value},
`;
  }
  generateMultipartPostData() {
    const postData = this.params.find(param => param.in === 'postData');
    if (!postData) return '';
    const formData = postData.value.split('&').map(param => param.split('='));
    const fields = formData.map(([name, value]) => {
      if (name.startsWith('@')) {
        const path = name.substring(1);
        return `    [
      'name' => '${value}',
      'contents' => fopen('${path}', 'r')
    ],`;
      } else {
        return `    [
      'name' => '${name}',
      'contents' => '${value}'
    ],`;
      }
    }).join('\n');
    return `  'multipart' => [
${fields}
  ],
`;
  }
  generateOtherPostData() {
    const postData = this.params.find(param => param.in === 'postData');
    if (!postData) return '';
    return `  'body' => '${postData.value}',
`;
  }
  generateCookie() {
    const cookieParams = this.params.filter(param => param.in === 'cookie');
    if (cookieParams.length === 0) return '';
    const cookieString = cookieParams.map(param => `${param.name}=${param.value}`).join(';');
    return `  'headers' => [
    'Cookie' => '${cookieString}',
  ],
`;
  }
}
/* harmony default export */ var Stippets_PhpGuzzleGenerator = (PhpGuzzleGenerator_PhpGuzzleGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/PowerShellWebRequestGenerator.js

class PowerShellWebRequestGenerator_PowerShellWebRequestGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `$response = Invoke-WebRequest -Uri "${this.baseUrl}${url}" -Method ${this.method} `;
  }
  generateFooterFile(url) {
    return `$response`;
  }
  generateMimeTypeHeader() {
    if (this.mimeType === "application/json") {
      return `-ContentType "application/json"`;
    } else if (this.mimeType === "multipart/form-data") {
      return `-ContentType "multipart/form-data"`;
    } else {
      return `-ContentType "application/x-www-form-urlencoded"`;
    }
  }
  generateHeaders() {
    const headerParams = this.params.filter(param => param.in === "headers");
    let headersCode = "";
    headerParams.forEach(param => {
      headersCode += `-Header @{"${param.name}"="${param.value}"}\n`;
    });
    return headersCode;
  }
  generateQueryParams() {
    const queryParams = this.params.filter(param => param.in === "query");
    let queryParamsCode = "";
    queryParams.forEach(param => {
      queryParamsCode += `'${param.name}'='${param.value}'&`;
    });
    if (queryParamsCode) {
      queryParamsCode = queryParamsCode.slice(0, -1);
      return `-Uri "${this.baseUrl}${this.url}?${queryParamsCode}"`;
    }
    return "";
  }
  generateJsonPostData() {
    const postDataParams = this.params.filter(param => param.in === "postData");
    let postDataCode = "";
    postDataParams.forEach(param => {
      postDataCode += `"${param.name}": "${param.value}",`;
    });
    if (postDataCode) {
      postDataCode = postDataCode.slice(0, -1);
      return `-Body '{"${postDataCode}}' ${this.generateMimeTypeHeader()}`;
    }
    return "";
  }
  generateMultipartPostData() {
    const postDataParams = this.params.filter(param => param.in === "postData" && param.type === "file");
    let multipartDataCode = "";
    postDataParams.forEach(param => {
      multipartDataCode += `-Form @{"${param.name}" = (Get-Item "${param.path}")};`;
    });
    if (multipartDataCode) {
      multipartDataCode = multipartDataCode.slice(0, -1);
      return `${multipartDataCode} ${this.generateMimeTypeHeader()}`;
    }
    return "";
  }
  generateOtherPostData() {
    const postDataParams = this.params.filter(param => param.in === "postData");
    let postDataCode = "";
    postDataParams.forEach(param => {
      postDataCode += `"${param.name}=${param.value}&"`;
    });
    if (postDataCode) {
      postDataCode = postDataCode.slice(0, -1);
      return `-Body "${postDataCode}" ${this.generateMimeTypeHeader()}`;
    }
    return "";
  }
  generateCookie() {
    const cookieParams = this.params.filter(param => param.in === "cookie");
    let cookieCode = "";
    cookieParams.forEach(param => {
      cookieCode += `-Cookie "${param.name}=${param.value}";`;
    });
    return cookieCode;
  }
}
/* harmony default export */ var Stippets_PowerShellWebRequestGenerator = (PowerShellWebRequestGenerator_PowerShellWebRequestGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/PowerShellRestMethodGenerator.js

class PowerShellRestMethodGenerator_PowerShellRestMethodGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `$response = Invoke-RestMethod -Uri "${this.baseUrl}${url}" -ContentType "${this.mimeType}" -Method ${this.method}\n`;
  }
  generateFooterFile(url) {
    return ``;
  }
  generateMimeTypeHeader() {
    return '';
  }
  generateHeaders() {
    let headers = '';
    this.params.filter(param => param.in === 'headers').forEach(param => {
      headers += ` -Headers @{${param.name}='${param.value}'}\n`;
    });
    return headers;
  }
  generateQueryParams() {
    let queryParams = '';
    this.params.filter(param => param.in === 'query').forEach(param => {
      queryParams += ` -Query @{${param.name}='${param.value}'}\n`;
    });
    return queryParams;
  }
  generateJsonPostData() {
    let jsonData = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      if (param.type === 'file') {
        jsonData += ` -Body (Get-Content "${param.path}" -Raw | ConvertFrom-Json)\n`;
      } else {
        jsonData += ` -Body '${param.value}'\n`;
      }
    });
    return jsonData;
  }
  generateMultipartPostData() {
    let multipartData = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      if (param.type === 'file') {
        multipartData += ` -Form @{${param.name}=(Get-Item "${param.path}").OpenRead()}\n`;
      } else {
        multipartData += ` -Form @{${param.name}='${param.value}'}\n`;
      }
    });
    return multipartData;
  }
  generateOtherPostData() {
    let otherData = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      otherData += ` -Body '${param.value}'\n`;
    });
    return otherData;
  }
  generateCookie() {
    let cookieData = '';
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      cookieData += ` -Headers @{Cookie='${param.name}=${param.value};'}\n`;
    });
    return cookieData;
  }
}
/* harmony default export */ var Stippets_PowerShellRestMethodGenerator = (PowerShellRestMethodGenerator_PowerShellRestMethodGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/RGenerator.js

class RGenerator_RGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `
      const requestOptions = {
        method: '${this.method}',
        headers: {
          'Content-Type': '${this.mimeType || ''}',
          ${this.hasHeadersParams() ? this.generateHeaders() : ''}
        }
      };

      fetch('${this.baseUrl}${url}', requestOptions)
        .then(response => response.json())
        .then(data => console.log(data))
        .catch(error => console.error(error));
    `;
  }
  generateFooterFile(url) {
    return '';
  }
  generateMimeTypeHeader() {
    return '';
  }
  generateHeaders() {
    return this.params.filter(param => param.in === 'headers').map(param => `'${param.name}': '${param.value}'`).join(',\n');
  }
  generateQueryParams() {
    const queryParams = this.params.filter(param => param.in === 'query');
    if (queryParams.length === 0) {
      return '';
    }
    const queryString = queryParams.map(param => `${param.name}=${param.value}`).join('&');
    return `const url = '${this.baseUrl}${this.url}?${queryString}';\n`;
  }
  generateJsonPostData() {
    const postData = this.params.find(param => param.in === 'postData');
    return `const data = ${postData.value};\n`;
  }
  generateMultipartPostData() {
    const boundary = '----WebKitFormBoundary7MA4YWxkTrZu0gW';
    const contentType = `multipart/form-data; boundary=${boundary}`;
    let body = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      body += `--${boundary}\r\n`;
      body += `Content-Disposition: form-data; name="${param.name}"\r\n`;
      body += '\r\n';
      body += `${param.value}\r\n`;
    });
    body += `--${boundary}--\r\n`;
    return `const requestOptions = {
    method: 'POST',
    headers: {
      'Content-Type': '${contentType}'
    },
    body: '${body}'
  };\n`;
  }
  generateOtherPostData() {
    const postData = this.params.find(param => param.in === 'postData');
    return `const data = '${postData.value}';\n`;
  }
  generateCookie() {
    const cookieParams = this.params.filter(param => param.in === 'cookie');
    const cookieString = cookieParams.map(param => `${param.name}=${param.value}`).join('; ');
    return `requestOptions.headers.Cookie = '${cookieString}';\n`;
  }
}
/* harmony default export */ var Stippets_RGenerator = (RGenerator_RGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/RubyGenerator.js

class RubyGenerator_RubyGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `require 'net/http'
require 'uri'

uri = URI.parse('${this.baseUrl}${url}')
`;
  }
  generateFooterFile(url) {
    return '';
  }
  generateMimeTypeHeader() {
    if (this.mimeType === 'multipart/form-data') {
      return "request['Content-Type'] = 'multipart/form-data'";
    } else if (this.mimeType === 'application/json') {
      return "request['Content-Type'] = 'application/json'";
    } else {
      return '';
    }
  }
  generateHeaders() {
    let headers = '';
    this.params.filter(param => param.in === 'headers').forEach(param => {
      headers += `request['${param.name}'] = '${param.value}'\n`;
    });
    return headers;
  }
  generateQueryParams() {
    let queryParams = '';
    this.params.filter(param => param.in === 'query').forEach((param, index) => {
      if (index === 0) {
        queryParams += '?';
      } else {
        queryParams += '&';
      }
      queryParams += `${param.name}=${param.value}`;
    });
    return queryParams;
  }
  generateJsonPostData() {
    let postData = '{';
    this.params.filter(param => param.in === 'postData').forEach((param, index) => {
      if (index > 0) {
        postData += ',';
      }
      postData += `"${param.name}": "${param.value}"`;
    });
    postData += '}';
    return `request.body = '${postData}'\n`;
  }
  generateMultipartPostData() {
    const boundary = '----WebKitFormBoundary7MA4YWxkTrZu0gW';
    const contentType = `multipart/form-data; boundary=${boundary}`;
    let body = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      if (param.type === 'file') {
        body += `--${boundary}\r\n`;
        body += `Content-Disposition: form-data; name="${param.name}"; filename="${param.path}"\r\n`;
        body += `Content-Type: ${contentType}\r\n`;
        body += '\r\n';
        body += 'file content';
        body += '\r\n';
      } else {
        body += `--${boundary}\r\n`;
        body += `Content-Disposition: form-data; name="${param.name}"\r\n`;
        body += '\r\n';
        body += `${param.value}\r\n`;
      }
    });
    body += `--${boundary}--\r\n`;
    return `request.body = '${body}'\n`;
  }
  generateOtherPostData() {
    let postData = '';
    this.params.filter(param => param.in === 'postData').forEach((param, index) => {
      if (index > 0) {
        postData += '&';
      }
      postData += `${param.name}=${param.value}`;
    });
    return `request.body = '${postData}'\n`;
  }
  generateCookie() {
    let cookie = '';
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      cookie += `request['Cookie'] = '${param.name}=${param.value}'\n`;
    });
    return cookie;
  }
}
/* harmony default export */ var Stippets_RubyGenerator = (RubyGenerator_RubyGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/RustGenerator.js

class RustGenerator_RustGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `use reqwest::header;
use std::collections::HashMap;

fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = reqwest::blocking::Client::new();
    let mut headers = header::HeaderMap::new();
    let mut url = "${this.baseUrl}${url}".to_string();
`;
  }
  generateFooterFile(url) {
    return `\n    let response = client.${this.method.toLowerCase()}(&url)
        .headers(headers)
        .send()?;
    println!("Status: {{}}", response.status());
    Ok(())
}`;
  }
  generateMimeTypeHeader() {
    if (this.mimeType === 'application/json') {
      return `    headers.insert(header::CONTENT_TYPE, header::HeaderValue::from_static("application/json"));`;
    } else if (this.mimeType === 'multipart/form-data') {
      return `    headers.insert(header::CONTENT_TYPE, header::HeaderValue::from_static("multipart/form-data"));`;
    } else {
      return `    headers.insert(header::CONTENT_TYPE, header::HeaderValue::from_static("application/x-www-form-urlencoded"));`;
    }
  }
  generateHeaders() {
    const headerParams = this.params.filter(param => param.in === 'headers');
    let headersCode = '';
    headerParams.forEach(param => {
      headersCode += `    headers.insert(header::HeaderName::from_static("${param.name}"), header::HeaderValue::from_static("${param.value}"));\n`;
    });
    return headersCode;
  }
  generateQueryParams() {
    const queryParams = this.params.filter(param => param.in === 'query');
    let queryParamsCode = '';
    queryParams.forEach(param => {
      queryParamsCode += `    url = url.replace("{${param.name}}", &${param.value}.to_string());\n`;
    });
    return queryParamsCode;
  }
  generateJsonPostData() {
    const postDataParams = this.params.filter(param => param.in === 'postData');
    let postDataCode = `    let data = json::json!({\n`;
    postDataParams.forEach(param => {
      postDataCode += `        "${param.name}": ${param.value},\n`;
    });
    postDataCode += `    });\n`;
    postDataCode += `    let body = serde_json::to_string(&data)?;\n`;
    postDataCode += `    let request = client.${this.method.toLowerCase()}(&url)\n`;
    postDataCode += `        .headers(headers)\n`;
    postDataCode += `        .body(body)\n`;
    postDataCode += `        .send()?;\n`;
    return postDataCode;
  }
  generateMultipartPostData() {
    const postDataParams = this.params.filter(param => param.in === 'postData');
    let postDataCode = `    let mut form = reqwest::multipart::Form::new();\n`;
    postDataParams.forEach(param => {
      if (param.type === 'file' && param.path) {
        postDataCode += `    let file = std::fs::File::open("${param.path}")?;\n`;
        postDataCode += `    let field = reqwest::multipart::Part::stream(reqwest::Body::wrap_stream(file)).file_name("${param.name}");\n`;
        postDataCode += `    form = form.part("${param.name}", field);\n`;
      } else {
        postDataCode += `    form = form.text("${param.name}", ${param.value});\n`;
      }
    });
    postDataCode += `    let response = client.${this.method.toLowerCase()}(&url)\n`;
    postDataCode += `        .headers(headers)\n`;
    postDataCode += `        .multipart(form)\n`;
    postDataCode += `        .send()?;\n`;
    return postDataCode;
  }
  generateOtherPostData() {
    const postDataParams = this.params.filter(param => param.in === 'postData');
    let postDataCode = `    let mut data = HashMap::new();\n`;
    postDataParams.forEach(param => {
      postDataCode += `    data.insert("${param.name}", ${param.value});\n`;
    });
    postDataCode += `    let request = client.${this.method.toLowerCase()}(&url)\n`;
    postDataCode += `        .headers(headers)\n`;
    postDataCode += `        .form(&data)\n`;
    postDataCode += `        .send()?;\n`;
    return postDataCode;
  }
  generateCookie() {
    const cookieParams = this.params.filter(param => param.in === 'cookie');
    let cookieCode = `    let mut cookie = String::new();
  `;
    cookieParams.forEach(param => {
      cookieCode += `    cookie.push_str("${param.name}=${param.value}; ");
    `;
    });
    cookieCode += `    headers.insert(header::COOKIE, header::HeaderValue::from_str(&cookie[..cookie.len() - 2])?);
  `;
    return cookieCode;
  }
}
/* harmony default export */ var Stippets_RustGenerator = (RustGenerator_RustGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/SwiftGenerator.js

class SwiftGenerator_SwiftGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `import Foundation\n\n`;
  }
  generateFooterFile(url) {
    return `let url = URL(string: "${this.baseUrl}${url}")!\n`;
  }
  generateMimeTypeHeader() {
    if (this.mimeType === 'application/json') {
      return `request.setValue("application/json", forHTTPHeaderField: "Content-Type")\n`;
    } else if (this.mimeType === 'multipart/form-data') {
      return `request.setValue("multipart/form-data", forHTTPHeaderField: "Content-Type")\n`;
    } else {
      return `request.setValue("${this.mimeType}", forHTTPHeaderField: "Content-Type")\n`;
    }
  }
  generateHeaders() {
    let code = '';
    this.params.filter(param => param.in === 'headers').forEach(param => {
      code += `request.setValue("${param.value}", forHTTPHeaderField: "${param.name}")\n`;
    });
    return code;
  }
  generateQueryParams() {
    let code = '';
    this.params.filter(param => param.in === 'query').forEach(param => {
      code += `let ${param.name} = "${param.value}"\n`;
    });
    return code;
  }
  generateJsonPostData() {
    let code = '';
    code += `let postData = try! JSONSerialization.data(withJSONObject: [`;
    this.params.filter(param => param.in === 'postData').forEach((param, index) => {
      code += `"${param.name}": "${param.value}"`;
      if (index !== this.params.filter(p => p.in === 'postData').length - 1) {
        code += ', ';
      }
    });
    code += `])\n`;
    code += `request.httpBody = postData\n`;
    return code;
  }
  generateMultipartPostData() {
    let code = '';
    code += `let boundary = UUID().uuidString\n`;
    code += `request.setValue("multipart/form-data; boundary=\\(boundary)", forHTTPHeaderField: "Content-Type")\n`;
    code += `let httpBody = NSMutableData()\n`;
    this.params.filter(param => param.in === 'postData').forEach(param => {
      if (param.type === 'file') {
        code += `let filePath = "${param.path}"\n`;
        code += `let filename = "${param.value}"\n`;
        code += `let fileData = try! Data(contentsOf: URL(fileURLWithPath: filePath))\n`;
        code += `let mimetype = "application/octet-stream"\n`;
        code += `httpBody.append("--\\(boundary)\\r\\n".data(using: .utf8)!)\n`;
        code += `httpBody.append("Content-Disposition: form-data; name=\\"${param.name}\\"; filename=\\"\\(filename)\\"\\r\\n".data(using: .utf8)!)\n`;
        code += `httpBody.append("Content-Type: \\(mimetype)\\r\\n\\r\\n".data(using: .utf8)!)\n`;
        code += `httpBody.append(fileData)\n`;
      } else {
        code += `httpBody.append("--\\(boundary)\\r\\n".data(using: .utf8)!)\n`;
        code += `httpBody.append("Content-Disposition: form-data; name=\\"${param.name}\\"\\r\\n\\r\\n".data(using: .utf8)!)\n`;
        code += `httpBody.append("${param.value}".data(using: .utf8)!)\n`;
      }
    });
    code += `httpBody.append("\\r\\n--\\(boundary)--\\r\\n".data(using: .utf8)!)\n`;
    code += `request.httpBody = httpBody as Data\n`;
    return code;
  }
  generateOtherPostData() {
    let code = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      code += `let ${param.name} = "${param.value}"\n`;
    });
    code += `let postData = ${this.params.filter(param => param.in === 'postData').map(param => param.name).join(' + ')}\n`;
    code += `request.httpBody = postData.data(using: .utf8)\n`;
    return code;
  }
  generateCookie() {
    let code = '';
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      code += `request.setValue("${param.value}", forHTTPHeaderField: "Cookie")\n`;
    });
    return code;
  }
}
/* harmony default export */ var Stippets_SwiftGenerator = (SwiftGenerator_SwiftGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/BrainfuckGenerator.js

class BrainfuckGenerator_BrainfuckGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return '';
  }
  generateFooterFile(url) {
    return '';
  }
  generateMimeTypeHeader() {
    return '';
  }
  generateHeaders() {
    return '';
  }
  generateQueryParams() {
    return '';
  }
  generateJsonPostData() {
    return '';
  }
  generateMultipartPostData() {
    return '';
  }
  generateOtherPostData() {
    return '';
  }
  generateCookie() {
    return '';
  }
  generateLoopStart() {
    return '[';
  }
  generateLoopEnd() {
    return ']';
  }
  generateMovePointer(amount) {
    if (amount > 0) {
      return '+'.repeat(amount);
    } else if (amount < 0) {
      return '-'.repeat(-amount);
    } else {
      return '';
    }
  }
  generateAddValue(amount) {
    if (amount > 0) {
      return '>'.repeat(amount) + '+';
    } else if (amount < 0) {
      return '>'.repeat(-amount) + '-';
    } else {
      return '';
    }
  }
  generateOutputValue() {
    return '.';
  }
  generateInputValue() {
    return ',';
  }
  generateComment(comment) {
    return `; ${comment}\n`;
  }
  generateBrainfuckCodeForParam(param) {
    switch (param.in) {
      case 'path':
        return this.generateComment(`Path parameter: ${param.name}`);
      case 'query':
        return this.generateComment(`Query parameter: ${param.name}`);
      case 'postData':
        return this.generateComment(`Post data parameter: ${param.name}`);
      case 'headers':
        return this.generateComment(`Header parameter: ${param.name}`);
      case 'cookie':
        return this.generateComment(`Cookie parameter: ${param.name}`);
      default:
        return '';
    }
  }
  generateBrainfuckCodeForUrl(url) {
    let code = '';
    for (let i = 0; i < url.length; i++) {
      const charCode = url.charCodeAt(i);
      code += this.generateAddValue(charCode) + this.generateOutputValue();
    }
    return code;
  }
  generateCode() {
    let url = this.url;
    this.params.filter(param => param.in === 'path').forEach(param => {
      url = url.replace(`{${param.name}}`, param.value);
    });
    let code = '';

    // Generate Brainfuck code for URL
    const urlCode = this.generateBrainfuckCodeForUrl(this.baseUrl + url);
    code += this.generateComment('------ URL ------\n');
    code += urlCode;
    code += '\n\n';

    // Generate Brainfuck code for parameters
    this.params.forEach(param => {
      const paramCode = this.generateBrainfuckCodeForParam(param);
      code += paramCode;
    });
    return code;
  }
}
/* harmony default export */ var Stippets_BrainfuckGenerator = (BrainfuckGenerator_BrainfuckGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/ArduinoGenerator.js

class ArduinoGenerator_ArduinoGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `#include <WiFi.h>
#include <HTTPClient.h>

const char* ssid = "your_SSID";
const char* password = "your_PASSWORD";

void setup() {
  Serial.begin(115200);
  WiFi.begin(ssid, password);
  while (WiFi.status() != WL_CONNECTED) {
    delay(1000);
    Serial.println("Connecting to WiFi...");
  }
  Serial.println("WiFi connected");
}

void loop() {
  HTTPClient http;
  http.begin("${this.baseUrl}${url}");
`;
  }
  generateFooterFile(url) {
    return `  http.end();
}\n`;
  }
  generateMimeTypeHeader() {
    if (this.mimeType === 'application/json') {
      return `  http.addHeader("Content-Type", "application/json");\n`;
    } else if (this.mimeType === 'multipart/form-data') {
      return `  http.addHeader("Content-Type", "multipart/form-data");\n`;
    } else {
      return '';
    }
  }
  generateHeaders() {
    let headersCode = '';
    this.params.filter(param => param.in === 'headers').forEach(param => {
      headersCode += `  http.addHeader("${param.name}", "${param.value}");\n`;
    });
    return headersCode;
  }
  generateQueryParams() {
    let queryParams = '';
    this.params.filter(param => param.in === 'query').forEach(param => {
      queryParams += `  http.addQueryParameter("${param.name}", "${param.value}");\n`;
    });
    return queryParams;
  }
  generateJsonPostData() {
    let jsonData = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      if (param.type === 'file') {
        jsonData += `  // File upload not supported in JSON data\n`;
      } else {
        jsonData += `  StaticJsonDocument<200> doc;\n`;
        jsonData += `  doc["${param.name}"] = "${param.value}";\n`;
        jsonData += `  String json;\n`;
        jsonData += `  serializeJson(doc, json);\n`;
        jsonData += `  http.sendRequest("POST", json);\n`;
      }
    });
    return jsonData;
  }
  generateMultipartPostData() {
    let multipartData = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      if (param.type === 'file') {
        multipartData += `  http.addFileToUpload("${param.path}", "${param.name}");\n`;
      } else {
        multipartData += `  http.addFormField("${param.name}", "${param.value}");\n`;
      }
    });
    return multipartData;
  }
  generateOtherPostData() {
    let postData = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      postData += `  http.addHeader("Content-Type", "application/x-www-form-urlencoded");\n`;
      postData += `  String data = "${param.name}=${param.value}";\n`;
      postData += `  http.sendRequest("POST", data);\n`;
    });
    return postData;
  }
  generateCookie() {
    let cookieCode = '';
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      cookieCode += `  http.addHeader("Cookie", "${param.name}=${param.value}");\n`;
    });
    return cookieCode;
  }
}
/* harmony default export */ var Stippets_ArduinoGenerator = (ArduinoGenerator_ArduinoGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/PhpHttp1Generator.js

class PhpHttp1Generator_PhpHttp1Generator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `
      <?php
      $ch = curl_init('${this.baseUrl}${url}');
      curl_setopt($ch, CURLOPT_RETURNTRANSFER, 1);
      curl_setopt($ch, CURLOPT_CUSTOMREQUEST, '${this.method}');
    `;
  }
  generateFooterFile(url) {
    return `
      $response = curl_exec($ch);
      curl_close($ch);
      echo $response;
      ?>
    `;
  }
  generateMimeTypeHeader() {
    switch (this.mimeType) {
      case 'application/json':
        return "curl_setopt($ch, CURLOPT_HTTPHEADER, array('Content-Type: application/json'));";
      case 'multipart/form-data':
        return "curl_setopt($ch, CURLOPT_HTTPHEADER, array('Content-Type: multipart/form-data'));";
      case 'application/x-www-form-urlencoded':
      default:
        return "curl_setopt($ch, CURLOPT_HTTPHEADER, array('Content-Type: application/x-www-form-urlencoded'));";
    }
  }
  generateHeaders() {
    const headers = this.params.filter(param => param.in === 'headers');
    const headersString = headers.map(header => `'${header.name}: ${header.value}'`).join(', ');
    return `curl_setopt($ch, CURLOPT_HTTPHEADER, array(${headersString}));`;
  }
  generateQueryParams() {
    const queryParams = this.params.filter(param => param.in === 'query');
    const queryString = queryParams.map(param => `${param.name}=${param.value}`).join('&');
    return `curl_setopt($ch, CURLOPT_URL, '${this.baseUrl}${this.url}?${queryString}');`;
  }
  generateJsonPostData() {
    const postData = this.params.find(param => param.in === 'postData');
    return `curl_setopt($ch, CURLOPT_POSTFIELDS, '${postData.value}');`;
  }
  generateMultipartPostData() {
    const postData = this.params.find(param => param.in === 'postData');
    const path = postData.path || '';
    return `
      $postFields = array(
        '${postData.name}' => new CURLFile('${path}')
      );
      curl_setopt($ch, CURLOPT_POSTFIELDS, $postFields);
    `;
  }
  generateOtherPostData() {
    const postData = this.params.find(param => param.in === 'postData');
    return `curl_setopt($ch, CURLOPT_POSTFIELDS, '${postData.value}');`;
  }
  generateCookie() {
    const cookieParams = this.params.filter(param => param.in === 'cookie');
    const cookieString = cookieParams.map(cookie => `${cookie.name}=${cookie.value}`).join('; ');
    return `curl_setopt($ch, CURLOPT_COOKIE, '${cookieString}');`;
  }
}
/* harmony default export */ var Stippets_PhpHttp1Generator = (PhpHttp1Generator_PhpHttp1Generator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/PhpHttp2Generator.js

class PhpHttp2Generator_PhpHttp2Generator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `<?php
use Http2\Headers;
use Http2\Request;
use Http2\TransportException;
use Http2\Client;
use Http2\Request\Method;

require_once 'vendor/autoload.php';

$client = new Client();

$request = new Request(
  Method::${this.method},
  '${this.baseUrl}${url}'
);`;
  }
  generateFooterFile(url) {
    return `
try {
  $response = $client->send($request);
  echo $response->getBody();
} catch (TransportException $e) {
  echo $e->getMessage();
}
?>`;
  }
  generateMimeTypeHeader() {
    let mimeTypeHeader = '';
    if (this.mimeType === 'application/json') {
      mimeTypeHeader = `$request->setHeader(Headers::CONTENT_TYPE, 'application/json');`;
    } else if (this.mimeType === 'multipart/form-data') {
      mimeTypeHeader = `$request->setHeader(Headers::CONTENT_TYPE, 'multipart/form-data');`;
    } else if (this.mimeType === 'application/x-www-form-urlencoded') {
      mimeTypeHeader = `$request->setHeader(Headers::CONTENT_TYPE, 'application/x-www-form-urlencoded');`;
    }
    return mimeTypeHeader;
  }
  generateHeaders() {
    let headers = '';
    this.params.filter(param => param.in === 'headers').forEach(param => {
      headers += `$request->setHeader('${param.name}', '${param.value}');\n`;
    });
    return headers;
  }
  generateQueryParams() {
    let queryParams = '';
    this.params.filter(param => param.in === 'query').forEach((param, index) => {
      if (index === 0) {
        queryParams += `$request->setUri('${this.baseUrl}${this.url}?${param.name}=${param.value}'`;
      } else {
        queryParams += `)->setQueryParam('${param.name}', '${param.value}')`;
      }
    });
    return queryParams + ');\n';
  }
  generateJsonPostData() {
    let jsonData = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      jsonData += `'${param.name}': '${param.value}',\n`;
    });
    return `$request->setBody(json_encode({${jsonData}}));\n`;
  }
  generateMultipartPostData() {
    let multipartData = '';
    this.params.filter(param => param.in === 'postData' && param.type === 'file').forEach(param => {
      multipartData += `$request->addMultipartData('${param.name}', '${param.path}');\n`;
    });
    this.params.filter(param => param.in === 'postData' && param.type !== 'file').forEach(param => {
      multipartData += `$request->addMultipartData('${param.name}', '${param.value}');\n`;
    });
    return multipartData;
  }
  generateOtherPostData() {
    let postData = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      postData += `'${param.name}': '${param.value}',\n`;
    });
    return `$request->setBody(http_build_query(array(${postData})), false);\n`;
  }
  generateCookie() {
    let cookieData = '';
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      cookieData += `$request->setCookie('${param.name}', '${param.value}');\n`;
    });
    return cookieData;
  }
}
/* harmony default export */ var Stippets_PhpHttp2Generator = (PhpHttp2Generator_PhpHttp2Generator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/JavaScriptTypescriptGenerator.js

class JavaScriptTypescriptGenerator_JavaScriptTypescriptGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `import axios, { AxiosRequestConfig } from 'axios';

const url = '${this.baseUrl}${url}';
const config: AxiosRequestConfig = {
  method: '${this.method}',
`;
  }
  generateFooterFile(url) {
    return `
};

axios(url, config)
  .then((response) => {
    console.log(response.data);
  })
  .catch((error) => {
    console.error(error);
  });
`;
  }
  generateMimeTypeHeader() {
    let headers = '';
    switch (this.mimeType) {
      case 'application/json':
        headers = `'Content-Type': 'application/json',`;
        break;
      case 'multipart/form-data':
        headers = `'Content-Type': 'multipart/form-data',`;
        break;
      case 'application/x-www-form-urlencoded':
        headers = `'Content-Type': 'application/x-www-form-urlencoded',`;
        break;
      default:
        headers = '';
    }
    return headers;
  }
  generateHeaders() {
    const headers = this.params.filter(param => param.in === 'headers').map(param => `'${param.name}': '${param.value}',`).join('\n  ');
    return headers;
  }
  generateQueryParams() {
    const params = this.params.filter(param => param.in === 'query').map(param => `'${param.name}': '${param.value}',`).join('\n  ');
    return `params: {
  ${params}
},`;
  }
  generateJsonPostData() {
    const data = this.params.filter(param => param.in === 'postData').map(param => `'${param.name}': '${param.value}',`).join('\n  ');
    return `data: {
  ${data}
},`;
  }
  generateMultipartPostData() {
    const data = this.params.filter(param => param.in === 'postData' && param.type === 'file').map(param => `{
  name: '${param.name}',
  filename: '${param.path}',
  data: fs.readFileSync('${param.path}'),
},`).join('\n  ');
    return `data: [
  ${data}
],`;
  }
  generateOtherPostData() {
    const data = this.params.filter(param => param.in === 'postData').map(param => `'${param.name}': '${param.value}',`).join('\n  ');
    return `data: {
  ${data}
},`;
  }
  generateCookie() {
    const cookies = this.params.filter(param => param.in === 'cookie').map(param => `${param.name}=${param.value}`).join(';');
    return `withCredentials: true,
headers: {
  'Cookie': '${cookies}',
},`;
  }
}
/* harmony default export */ var Stippets_JavaScriptTypescriptGenerator = (JavaScriptTypescriptGenerator_JavaScriptTypescriptGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/ObjectCGenerator.js

class ObjectCGenerator_ObjectCGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `NSMutableURLRequest *request = [NSMutableURLRequest requestWithURL:[NSURL URLWithString:@"${this.baseUrl}${url}"]];
`;
  }
  generateFooterFile(url) {
    return `NSURLSessionDataTask *task = [session dataTaskWithRequest:request completionHandler:^(NSData *data, NSURLResponse *response, NSError *error) {
    // handle response
}];
[task resume];`;
  }
  generateMimeTypeHeader() {
    return `NSString *contentType = @"${this.mimeType}";
[request setValue:contentType forHTTPHeaderField:@"Content-Type"];
`;
  }
  generateHeaders() {
    let headers = '';
    this.params.filter(param => param.in === 'headers').forEach(param => {
      headers += `[request setValue:@"${param.value}" forHTTPHeaderField:@"${param.name}"];
`;
    });
    return headers;
  }
  generateQueryParams() {
    let queryParams = '';
    let isFirst = true;
    this.params.filter(param => param.in === 'query').forEach(param => {
      if (isFirst) {
        queryParams += `[url appendString:@"?${param.name}=${param.value}"];
`;
        isFirst = false;
      } else {
        queryParams += `[url appendString:@"&${param.name}=${param.value}"];
`;
      }
    });
    return queryParams;
  }
  generateJsonPostData() {
    return `NSData *postData = [NSJSONSerialization dataWithJSONObject:${JSON.stringify(this.getPostDataParamsValues())} options:NSJSONWritingPrettyPrinted error:&error];
[request setHTTPBody:postData];
`;
  }
  generateMultipartPostData() {
    return `NSData *postData = [self createMultipartFormData:${JSON.stringify(this.getPostDataParamsValues())}];
[request setHTTPBody:postData];
`;
  }
  generateOtherPostData() {
    let postData = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      postData += `[request setHTTPBody:[@"${param.value}" dataUsingEncoding:NSUTF8StringEncoding]];
`;
    });
    return postData;
  }
  generateCookie() {
    let cookieParams = '';
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      cookieParams += `[request addValue:@"${param.value}" forHTTPHeaderField:@"Cookie"];
`;
    });
    return cookieParams;
  }
  getPostDataParamsValues() {
    let postDataParams = {};
    this.params.filter(param => param.in === 'postData').forEach(param => {
      if (param.type === 'file') {
        postDataParams[param.name] = `[NSURL fileURLWithPath:@"${param.path}"]`;
      } else {
        postDataParams[param.name] = param.value;
      }
    });
    return postDataParams;
  }
}
/* harmony default export */ var Stippets_ObjectCGenerator = (ObjectCGenerator_ObjectCGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/CppGenerator.js

class CppGenerator_CppGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `#include <iostream>
#include <string>
#include <curl/curl.h>

int main() {
  CURL *curl;
  CURLcode res;
  std::string url = "${this.baseUrl}${url}";
`;
  }
  generateFooterFile(url) {
    return `
  return 0;
}`;
  }
  generateMimeTypeHeader() {
    return `  curl_mime *mime = curl_mime_init(curl);
  curl_mime_type(mime, "${this.mimeType}");
  curl_easy_setopt(curl, CURLOPT_MIMEPOST, mime);
`;
  }
  generateHeaders() {
    let headers = '';
    this.params.filter(param => param.in === 'headers').forEach(param => {
      headers += `  curl_slist *headers = nullptr;
  headers = curl_slist_append(headers, "${param.name}: ${param.value}");
  curl_easy_setopt(curl, CURLOPT_HTTPHEADER, headers);
`;
    });
    return headers;
  }
  generateQueryParams() {
    let queryParams = '';
    this.params.filter(param => param.in === 'query').forEach(param => {
      queryParams += `  url += "?${param.name}=${param.value}";
`;
    });
    return queryParams;
  }
  generateJsonPostData() {
    let json = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      json += `"${param.name}": "${param.value}", `;
    });
    json = json.slice(0, -2);
    return `  std::string data = "{${json}}";
  curl_easy_setopt(curl, CURLOPT_POSTFIELDS, data.c_str());
  curl_easy_setopt(curl, CURLOPT_POSTFIELDSIZE, data.length());
`;
  }
  generateMultipartPostData() {
    let multipart = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      if (param.type === 'file' && param.path) {
        multipart += `  curl_mimepart *part${param.name} = curl_mime_addpart(mime);
  curl_mime_name(part${param.name}, "${param.name}");
  curl_mime_filedata(part${param.name}, "${param.path}");
`;
      } else {
        multipart += `  curl_mimepart *part${param.name} = curl_mime_addpart(mime);
  curl_mime_name(part${param.name}, "${param.name}");
  curl_mime_data(part${param.name}, "${param.value}", CURL_ZERO_TERMINATED);
`;
      }
    });
    return multipart;
  }
  generateOtherPostData() {
    let postData = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      postData += `  std::string data = "${param.name}=${param.value}";
  curl_easy_setopt(curl, CURLOPT_POSTFIELDS, data.c_str());
  curl_easy_setopt(curl, CURLOPT_POSTFIELDSIZE, data.length());
`;
    });
    return postData;
  }
  generateCookie() {
    let cookie = '';
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      cookie += `  curl_easy_setopt(curl, CURLOPT_COOKIE, "${param.name}=${param.value}");
`;
    });
    return cookie;
  }
}
/* harmony default export */ var Stippets_CppGenerator = (CppGenerator_CppGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/DartGenerator.js

class DartGenerator_DartGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `import 'dart:convert';
import 'package:http/http.dart' as http;

void main() async {
  var url = '${this.baseUrl}${url}';
`;
  }
  generateFooterFile(url) {
    return `}
`;
  }
  generateMimeTypeHeader() {
    return `  var headers = {'Content-Type': '${this.mimeType}'};
`;
  }
  generateHeaders() {
    let headersCode = '';
    this.params.filter(param => param.in === 'headers').forEach(param => {
      headersCode += `  headers['${param.name}'] = '${param.value}';
`;
    });
    return headersCode;
  }
  generateQueryParams() {
    let queryParamsCode = '';
    this.params.filter(param => param.in === 'query').forEach(param => {
      queryParamsCode += `  url = Uri.parse(url).replace(queryParameters: {'${param.name}': '${param.value}'}).toString();
`;
    });
    return queryParamsCode;
  }
  generateJsonPostData() {
    let jsonData = {};
    this.params.filter(param => param.in === 'postData').forEach(param => {
      if (param.type === 'file') {
        return `File type is not supported for Content-Type: ${this.mimeType}`;
      } else {
        jsonData[param.name] = param.value;
      }
    });
    return `  var body = json.encode(${JSON.stringify(jsonData)});
`;
  }
  generateMultipartPostData() {
    return 'multipart/form-data is not supported yet';
  }
  generateOtherPostData() {
    let postDataCode = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      if (param.type === 'file') {
        return `File type is not supported for Content-Type: ${this.mimeType}`;
      } else {
        postDataCode += `  var body = '${param.name}=${param.value}';
`;
      }
    });
    return postDataCode;
  }
  generateCookie() {
    let cookieCode = '';
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      cookieCode += `  headers['Cookie'] = '${param.name}=${param.value}';
`;
    });
    return cookieCode;
  }
  generateCode() {
    let url = this.url;
    this.params.filter(param => param.in === 'path').forEach(param => {
      url = url.replace(`{${param.name}}`, param.value);
    });
    let fetchCode = this.generateHeaderFile(url);
    if (this.hasCookieParams()) {
      fetchCode += this.generateCookie();
    }
    if (this.hasMimeType()) {
      fetchCode += this.generateMimeTypeHeader();
    }
    if (this.hasHeadersParams()) {
      fetchCode += this.generateHeaders();
    }
    if (this.hasQueryParams()) {
      fetchCode += this.generateQueryParams();
    } else if (this.hasPostDataParams()) {
      if (this.mimeType === 'application/json') {
        fetchCode += this.generateJsonPostData();
      } else if (this.mimeType === 'multipart/form-data') {
        fetchCode += this.generateMultipartPostData();
      } else {
        fetchCode += this.generateOtherPostData();
      }
    }
    fetchCode += `  var response = await http.${this.method.toLowerCase()}(url, headers: headers, body: body);

  print(response.body);
}`;
    return fetchCode;
  }
}
/* harmony default export */ var Stippets_DartGenerator = (DartGenerator_DartGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/ScalaGenerator.js

class ScalaGenerator_ScalaGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `import scalaj.http.Http
import scalaj.http.HttpResponse

object Main {
def main(args: Array[String]): Unit = {
val response: HttpResponse[String] = Http("${this.baseUrl}${url}")`;
  }
  generateFooterFile(url) {
    return `\n     .asString     \nprintln(response.body)\n   } \n}`;
  }
  generateMimeTypeHeader() {
    if (this.mimeType === 'application/json') {
      return `\n     .header("Content-Type", "application/json")`;
    } else if (this.mimeType === 'multipart/form-data') {
      return `\n     .header("Content-Type", "multipart/form-data")`;
    } else {
      return `\n     .header("Content-Type", "application/x-www-form-urlencoded")`;
    }
  }
  generateHeaders() {
    let headersCode = '';
    this.params.filter(param => param.in === 'headers').forEach(param => {
      headersCode += `\n     .header("${param.name}", "${param.value}")\n`;
    });
    return headersCode;
  }
  generateQueryParams() {
    let queryParamsCode = '';
    this.params.filter(param => param.in === 'query').forEach(param => {
      queryParamsCode += `\n     .param('${param.name}', '${param.value}')\n`;
    });
    return queryParamsCode;
  }
  generateJsonPostData() {
    let postData = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      if (param.type === 'file') {
        postData += `${param.name}=@${param.path}`;
      } else {
        postData += `${param.name}=${param.value}`;
      }
    });
    return `\n     .postData(${postData})`;
  }
  generateMultipartPostData() {
    let postData = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      if (param.type === 'file') {
        postData += `\n     .postMulti(MultiPart("${param.name}", "${param.path}"))`;
      } else {
        postData += `\n     .postMulti(MultiPart("${param.name}", "${param.value}"))`;
      }
    });
    return postData;
  }
  generateOtherPostData() {
    let postData = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      if (param.type === 'file') {
        postData += `\n     .postData(("${param.name}", "${param.path}"))`;
      } else {
        postData += `\n     .postData(("${param.name}", "${param.value}"))`;
      }
    });
    return postData;
  }
  generateCookie() {
    let cookieCode = '';
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      cookieCode += `\n     .cookie("${param.name}", "${param.value}")\n`;
    });
    return cookieCode;
  }
}
/* harmony default export */ var Stippets_ScalaGenerator = (ScalaGenerator_ScalaGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/LuaGenerator.js

class LuaGenerator_LuaGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    return `-- ${this.method} ${this.baseUrl}${url}\n`;
  }
  generateFooterFile() {
    return '';
  }
  generateMimeTypeHeader() {
    return '';
  }
  generateHeaders() {
    let headers = '';
    this.params.filter(param => param.in === 'headers').forEach(param => {
      headers += `${param.name}: ${param.value}\n`;
    });
    return headers;
  }
  generateQueryParams() {
    let queryParams = '';
    this.params.filter(param => param.in === 'query').forEach(param => {
      queryParams += `${param.name}=${param.value}&`;
    });
    queryParams = queryParams.slice(0, -1);
    return queryParams;
  }
  generateJsonPostData() {
    let data = '{';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      data += `"${param.name}": "${param.value}",`;
    });
    data = data.slice(0, -1);
    data += '}';
    return data;
  }
  generateMultipartPostData() {
    return 'Not implemented for Lua';
  }
  generateOtherPostData() {
    let data = '';
    this.params.filter(param => param.in === 'postData').forEach(param => {
      data += `${param.value}\n`;
    });
    return data;
  }
  generateCookie() {
    let cookie = '';
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      cookie += `${param.name}=${param.value}; `;
    });
    cookie = cookie.slice(0, -2);
    return `Cookie: ${cookie}\n`;
  }
  generateCode() {
    let url = this.url;
    this.params.filter(param => param.in === 'path').forEach(param => {
      url = url.replace(`{${param.name}}`, param.value);
    });
    let fetchCode = this.generateHeaderFile(url);
    if (this.hasCookieParams()) {
      fetchCode += this.generateCookie();
    }
    if (this.hasHeadersParams()) {
      fetchCode += this.generateHeaders();
    }
    if (this.hasQueryParams()) {
      url += `?${this.generateQueryParams()}`;
    } else if (this.hasPostDataParams()) {
      if (this.mimeType === 'application/json') {
        fetchCode += `Content-Type: application/json\n`;
        fetchCode += `Content-Length: ${this.generateJsonPostData().length}\n\n`;
        fetchCode += `${this.generateJsonPostData()}`;
      } else {
        fetchCode += `Content-Type: ${this.mimeType}\n\n`;
        fetchCode += `${this.generateOtherPostData()}`;
      }
    }
    fetchCode += this.generateFooterFile();
    return fetchCode;
  }
}
/* harmony default export */ var Stippets_LuaGenerator = (LuaGenerator_LuaGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/Stippets/PerlGenerator.js

class PerlGenerator_PerlGenerator extends _CodeGenerator {
  generateHeaderFile(url) {
    let code = '#!/usr/bin/perl\n';
    code += 'use LWP::UserAgent;\n';
    code += `my $url = "${this.baseUrl}${url}";\n`;
    code += `my $method = "${this.method.toLowerCase()}";\n`;
    code += 'my $ua = LWP::UserAgent->new();\n';
    return code;
  }
  generateFooterFile(url) {
    let code = '';
    code += 'my $response = $ua->request($request);\n';
    code += 'print $response->content();\n';
    return code;
  }
  generateMimeTypeHeader() {
    return '';
  }
  generateHeaders() {
    let code = '';
    this.params.filter(param => param.in === 'headers').forEach(param => {
      code += `$request->header("${param.name}" => "${param.value}");\n`;
    });
    return code;
  }
  generateQueryParams() {
    let code = '';
    this.params.filter(param => param.in === 'query').forEach(param => {
      code += `$url .= '?' if ($url !~ /\\?/);\n`;
      code += `$url .= '${param.name}=${param.value}&';\n`;
    });
    return code;
  }
  generateJsonPostData() {
    let code = '';
    const postData = this.params.find(param => param.in === 'postData');
    code += `$request->header('Content-Type' => 'application/json');\n`;
    code += `my $json = '${postData.value}';\n`;
    code += 'my $data = decode_json($json);\n';
    code += '$request->content($json);\n';
    return code;
  }
  generateMultipartPostData() {
    let code = '';
    const postData = this.params.find(param => param.in === 'postData');
    code += '$request->header(\'Content-Type\' => \'multipart/form-data\');\n';
    code += `my $filename = '${postData.path}';\n`;
    code += 'open(my $fh, ' < ', $filename) or die "Can\'t open $filename: $!";\n';
    code += 'binmode($fh);\n';
    code += 'my $data;\n';
    code += 'while(<$fh>) {\n';
    code += ' $data .= $_;\n';
    code += '}\n';
    code += 'close($fh);\n';
    code += '$request->content($data);\n';
    return code;
  }
  generateOtherPostData() {
    let code = '';
    const postData = this.params.find(param => param.in === 'postData');
    code += '$request->header(\'Content-Type\' => \'${this.mimeType}\');\n';
    code += `my $data = '${postData.value}';\n`;
    code += '$request->content($data);\n';
    return code;
  }
  generateCookie() {
    let code = '';
    this.params.filter(param => param.in === 'cookie').forEach(param => {
      code += `$request->header('Cookie', '${param.name}=${param.value}');\n`;
    });
    return code;
  }
}
/* harmony default export */ var Stippets_PerlGenerator = (PerlGenerator_PerlGenerator);
// CONCATENATED MODULE: ../components/lib/CodeGenerator/index.js









































class CodeGenerator_CodeGenerator {
  constructor() {
    this._baseUrl = '';
    this._url = '';
    this._method = 'GET';
    this._params = [];
    this._mimeType = 'application/x-www-form-urlencoded';
    this._lang = 'javascript';
    this._library = 'xmlhttprequest';
  }
  library(value) {
    this._library = value;
    return this;
  }
  lang(value) {
    this._lang = value;
    return this;
  }
  mimeType(value) {
    this._mimeType = value;
    return this;
  }
  params(value) {
    this._params = value;
    return this;
  }
  method(value) {
    this._method = value;
    return this;
  }
  url(value) {
    value = value.startsWith('/') ? value.substring(1) : value;
    this._url = value;
    return this;
  }
  baseUrl(value) {
    if (!value.endsWith('/')) {
      value += '/';
    }
    this._baseUrl = value;
    return this;
  }
  convertParams() {
    return {
      baseUrl: this._baseUrl,
      method: this._method,
      mimeType: this._mimeType,
      params: this._params,
      url: this._url
    };
  }
  convert() {
    switch (this._lang) {
      case 'shell':
        switch (this._library) {
          case 'curl':
            return new Stippets_ShellCurlGenerator(this.convertParams()).generateCode();
          case 'httpie':
            return new Stippets_ShellHttpieGenerator(this.convertParams()).generateCode();
          case 'wget':
            return new Stippets_ShellWgetGenerator(this.convertParams()).generateCode();
          default:
            throw new Error('bad library');
        }
      case 'javascript':
        switch (this._library) {
          case 'xmlhttprequest':
            return new Stippets_JavaScriptXMLHttpRequestGenerator(this.convertParams()).generateCode();
          case 'jquery':
            return new Stippets_JavaScriptJQueryGenerator(this.convertParams()).generateCode();
          case 'fetch':
            return new Stippets_JavaScriptFetchGenerator(this.convertParams()).generateCode();
          case 'axios':
            return new Stippets_JavaScriptAxiosGenerator(this.convertParams()).generateCode();
          case 'typescript':
            return new Stippets_JavaScriptTypescriptGenerator(this.convertParams()).generateCode();
          default:
            throw new Error('bad library');
        }
      case 'python':
        switch (this._library) {
          case 'python':
            return new Stippets_Python3Generator(this.convertParams()).generateCode();
          case 'requests':
            return new Stippets_PythonRequestsGenerator(this.convertParams()).generateCode();
          default:
            throw new Error('bad library');
        }
      case 'go':
        return new Stippets_GoGenerator(this.convertParams()).generateCode();
      case 'c':
        return new Stippets_CGenerator(this.convertParams()).generateCode();
      case 'ocaml':
        return new Stippets_OcamlGenerator(this.convertParams()).generateCode();
      case 'csharp':
        switch (this._library) {
          case 'httpclient':
            return new CSharpHttpclientGenerator(this.convertParams()).generateCode();
          case 'restsharp':
            return new Stippets_CSharpRestSharpGenerator(this.convertParams()).generateCode();
          default:
            throw new Error('bad library');
        }
      case 'java':
        switch (this._library) {
          case 'asynchttp':
            return new Stippets_JavaAsyncHttpClientGenerator(this.convertParams()).generateCode();
          case 'nethttp':
            return new Stippets_JavaNetHttpClientGenerator(this.convertParams()).generateCode();
          case 'okhttp':
            return new Stippets_JavaOkHttpGenerator(this.convertParams()).generateCode();
          case 'unirest':
            return new Stippets_JavaUnirestGenerator(this.convertParams()).generateCode();
          default:
            throw new Error('bad library');
        }
      case 'http':
        return new Stippets_HttpUrlStringGenerator(this.convertParams()).generateCode();
      case 'clojure':
        return new ClojureHttpGenerator(this.convertParams()).generateCode();
      case 'kotlin':
        return new Stippets_KotlinGenerator(this.convertParams()).generateCode();
      case 'php':
        switch (this._library) {
          case 'http1':
            return new Stippets_PhpHttp1Generator(this.convertParams()).generateCode();
          case 'http2':
            return new Stippets_PhpHttp2Generator(this.convertParams()).generateCode();
          case 'curl':
            return new Stippets_PhpCurlGenerator(this.convertParams()).generateCode();
          case 'guzzle':
            return new Stippets_PhpGuzzleGenerator(this.convertParams()).generateCode();
          default:
            throw new Error('bad library');
        }
      case 'powershell':
        switch (this._library) {
          case 'webrequest':
            return new Stippets_PowerShellWebRequestGenerator(this.convertParams()).generateCode();
          case 'restmethod':
            return new Stippets_PowerShellRestMethodGenerator(this.convertParams()).generateCode();
          default:
            throw new Error('bad library');
        }
      case 'r':
        return new Stippets_RGenerator(this.convertParams()).generateCode();
      case 'ruby':
        return new Stippets_RubyGenerator(this.convertParams()).generateCode();
      case 'rust':
        return new Stippets_RustGenerator(this.convertParams()).generateCode();
      case 'swift':
        return new Stippets_SwiftGenerator(this.convertParams()).generateCode();
      case 'objectivec':
        return new Stippets_ObjectCGenerator(this.convertParams()).generateCode();
      case 'cpp':
        switch (this._library) {
          case 'iostream':
            return new Stippets_CppGenerator(this.convertParams()).generateCode();
          case 'arduino':
            return new Stippets_ArduinoGenerator(this.convertParams()).generateCode();
          default:
            throw new Error('bad library');
        }
      case 'dart':
        return new Stippets_DartGenerator(this.convertParams()).generateCode();
      case 'scala':
        return new Stippets_ScalaGenerator(this.convertParams()).generateCode();
      case 'lua':
        return new Stippets_LuaGenerator(this.convertParams()).generateCode();
      case 'perl':
        return new Stippets_PerlGenerator(this.convertParams()).generateCode();
      case 'brainfuck':
        return new Stippets_BrainfuckGenerator(this.convertParams()).generateCode();
      default:
        throw new Error('bad lang');
    }
  }
}
// CONCATENATED MODULE: ./node_modules/babel-loader/lib??ref--2-0!./node_modules/@nuxt/components/dist/loader.js??ref--0-0!./node_modules/vue-loader/lib??vue-loader-options!../components/lib/CodeSimples.vue?vue&type=script&lang=js&



/* harmony default export */ var CodeSimplesvue_type_script_lang_js_ = ({
  name: 'code-simples',
  components: {
    CustomDropdownWithSubMenu: CustomDropdownWithSubMenu,
    CodeView: CodeView
  },
  props: {
    simples: {
      type: [Object, Array],
      required: false
    },
    baseUrl: {
      type: String,
      required: false,
      default: ''
    },
    url: {
      type: String,
      required: true
    },
    method: {
      type: String,
      required: false,
      default: 'GET'
    },
    params: {
      type: Array,
      required: false,
      default: () => []
    },
    mimeType: {
      type: String,
      required: false,
      default: 'application/x-www-form-urlencoded'
    },
    configs: {
      type: Array,
      required: false,
      default: function () {
        return [{
          snippet: 'shell',
          libraries: {
            cURL: 'curl',
            HTTPie: 'httpie',
            Wget: 'wget'
          }
        }, {
          snippet: 'javascript',
          libraries: {
            XMLHttpRequest: 'xmlhttprequest',
            jQuery: 'jquery',
            Fetch: 'fetch',
            Axios: 'axios'
          }
        }, {
          snippet: 'python',
          libraries: {
            'Python 3': 'python',
            'Requests': 'requests'
          }
        }, {
          snippet: 'go'
        }, {
          snippet: 'c'
        }, {
          snippet: 'ocaml'
        }, {
          snippet: 'csharp',
          libraries: {
            HttpClient: 'httpclient',
            RestSharp: 'restsharp'
          }
        }, {
          snippet: 'java',
          libraries: {
            AsyncHttp: 'asynchttp',
            NetHttp: 'nethttp',
            OkHttp: 'okhttp',
            Unirest: 'unirest'
          }
        }, {
          snippet: 'http'
        }, {
          snippet: 'clojure'
        }, {
          snippet: 'kotlin'
        }, {
          snippet: 'php',
          libraries: {
            cURL: 'curl',
            guzzle: 'guzzle',
            http1: 'http 1.1',
            http2: 'http 2'
          }
        }, {
          snippet: 'powershell',
          libraries: {
            WebRequest: 'webrequest',
            RestMethod: 'restmethod'
          }
        }, {
          snippet: 'r'
        }, {
          snippet: 'ruby'
        }, {
          snippet: 'rust'
        }, {
          snippet: 'swift'
        }, {
          snippet: 'objectivec'
        }, {
          snippet: 'cpp',
          libraries: {
            iostream: 'iostream',
            arduino: 'arduino'
          }
        }, {
          snippet: 'dart'
        }, {
          snippet: 'scala'
        }, {
          snippet: 'lua'
        }, {
          snippet: 'perl'
        }, {
          snippet: 'swift'
        }, {
          snippet: 'brainfuck'
        }];
      }
    }
  },
  data() {
    return {
      snippetIndex: 1,
      snippetLibraryIndex: 'XMLHttpRequest',
      code: '',
      showPopup: false,
      onPopupIndex: null,
      vals: []
    };
  },
  watch: {},
  methods: {
    onLangClick(snippet, library = null) {
      this.snippetIndex = snippet;
      this.snippetLibraryIndex = library;
      this.genCode();
    },
    genCode() {
      const method = this.method.toUpperCase();
      const generator = new CodeGenerator_CodeGenerator();
      generator.baseUrl(this.baseUrl);
      generator.url(this.url);
      generator.method(method);
      generator.params(JSON.parse(JSON.stringify(this.params)));
      generator.mimeType(this.mimeType);
      generator.lang(this.snippet);
      generator.library(this.snippetLibrary);
      this.code = generator.convert();
    }
  },
  computed: {
    snippet() {
      if (this.snippetIndex === null || !this.configs[this.snippetIndex]) return null;
      return this.configs[this.snippetIndex].snippet;
    },
    snippetLibrary() {
      if (this.snippetIndex === null || !this.configs[this.snippetIndex]) return null;
      if (this.snippetLibraryIndex === null) return null;
      if (!this.configs[this.snippetIndex].libraries) return null;
      return this.configs[this.snippetIndex].libraries[this.snippetLibraryIndex];
    }
  },
  mounted() {
    this.genCode();
  }
});
// CONCATENATED MODULE: ../components/lib/CodeSimples.vue?vue&type=script&lang=js&
 /* harmony default export */ var lib_CodeSimplesvue_type_script_lang_js_ = (CodeSimplesvue_type_script_lang_js_); 
// CONCATENATED MODULE: ../components/lib/CodeSimples.vue



function CodeSimples_injectStyles (context) {
  
  var style0 = __webpack_require__(106)
if (style0.__inject__) style0.__inject__(context)

}

/* normalize component */

var CodeSimples_component = Object(componentNormalizer["a" /* default */])(
  lib_CodeSimplesvue_type_script_lang_js_,
  render,
  staticRenderFns,
  false,
  CodeSimples_injectStyles,
  null,
  "30966e41"
  
)

/* harmony default export */ var CodeSimples = __webpack_exports__["default"] = (CodeSimples_component.exports);

/***/ }),

/***/ 65:
/***/ (function(module, exports, __webpack_require__) {

// style-loader: Adds some css to the DOM by adding a <style> tag

// load the styles
var content = __webpack_require__(103);
if(content.__esModule) content = content.default;
if(typeof content === 'string') content = [[module.i, content, '']];
if(content.locals) module.exports = content.locals;
// add CSS to SSR context
var add = __webpack_require__(7).default
module.exports.__inject__ = function (context) {
  add("dead3f62", content, true, context)
};

/***/ }),

/***/ 66:
/***/ (function(module, exports, __webpack_require__) {

// style-loader: Adds some css to the DOM by adding a <style> tag

// load the styles
var content = __webpack_require__(105);
if(content.__esModule) content = content.default;
if(typeof content === 'string') content = [[module.i, content, '']];
if(content.locals) module.exports = content.locals;
// add CSS to SSR context
var add = __webpack_require__(7).default
module.exports.__inject__ = function (context) {
  add("6ebe6ba2", content, true, context)
};

/***/ }),

/***/ 67:
/***/ (function(module, exports, __webpack_require__) {

// style-loader: Adds some css to the DOM by adding a <style> tag

// load the styles
var content = __webpack_require__(107);
if(content.__esModule) content = content.default;
if(typeof content === 'string') content = [[module.i, content, '']];
if(content.locals) module.exports = content.locals;
// add CSS to SSR context
var add = __webpack_require__(7).default
module.exports.__inject__ = function (context) {
  add("ae8656f0", content, true, context)
};

/***/ })

};;
//# sourceMappingURL=5.js.map